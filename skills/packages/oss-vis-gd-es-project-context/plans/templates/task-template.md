# Task · <日期>-<编号>-<slug>

> **使用方式**：复制本文件为 `task-YYYY-MM-DD-NNN-<slug>.md`，填入实际内容，同时在 `current-sprint.md` 追加索引。
>
> **繁简**：默认只填「元信息 + 目标 + 步骤 + 归档清单」。复杂任务再展开下方「可选强化」块；用不上就整段删掉。
>
> **工作流**：两阶段硬门槛见 `env/rules/03-task-workflow.md`。

---

## 任务元信息

| 项 | 值 |
| --- | --- |
| 编号 | `task-YYYY-MM-DD-NNN-<slug>` |
| Status | 待审批 / 已批准 / 进行中 / 已完成 / 已拒绝 |
| 类型 | <docs / skill / 编码 / 其它> |
| 影响范围 | <要修改/创建的文件路径> |
| Roadmap | <`plans/roadmap.md` 中的对应条目> |
| 验收标准 | <一句话；复杂任务改用下方验收 checklist> |
| 前置依赖 | <依赖的 task 编号或决策，可空> |

---

## 目标（Goal）

> 用 1~2 句话说明任务要解决什么问题。

---

## 步骤（执行计划）

> 每步明确「做什么 / 输出什么 / 是否需要停下来」。需要用户介入的步骤标「等待用户」。
> 典型节奏：澄清与结构化 → **等待用户（硬门槛①）** → 技术设计/编码/验证 → **等待用户（硬门槛②）** → 归档。

### 步骤 1：<标题>

- **动作**：<具体做什么>
- **输出**：<产出物路径或描述>
- **等待用户**：否 / 是

---

<!-- ========== 可选强化（复杂任务按需保留；简单任务整段删除）========== -->

## 背景与动机（可选）

> 为何现在做；关联的产品/技术背景。

---

## 用户故事（可选）

> As a… I want… So that…（按角色分条）

---

## 功能列表（可选）

- F1：<必须做>
- F2：

---

## 不做清单（可选 · 复杂任务建议非空）

- <明确排除项，防范围蔓延>

---

## 非功能需求（可选）

> 性能 / 兼容 / 依赖版本等约束。

---

## 业务规则（可选）

> 数据映射、状态流转、特殊逻辑。

---

## 前置 / 必读（可选）

- [ ] 已读：<路径>
- [ ] 依赖已就绪：<task-… / 决策 / 环境>

---

## 开放问题（可选）

| # | 问题 | 负责人 | 状态 | 结论 |
| - | ---- | ------ | ---- | ---- |
| 1 |      |        | 开放 / 已关闭 |  |

---

## 验收 checklist（可选）

- [ ] A1：<可独立判定的验收项>
- [ ] A2：
- [ ] 影响架构 / 硬约束的决策已回写 `design/`（无则 N/A）
- [ ] `env/AGENTS.md` 声明的质量门槛命令已通过（无则 N/A）

---

## 技术设计（可选 · 阶段二回填）

> 改动点清单、依赖顺序、复用判断、风险标注。模块级演进可另写 `design/` 并在此链到路径。

---

## 变更记录（可选 · 回退/需求变更时追加）

| 日期 | 变更 | 原因 | 是否重新过硬门槛① |
| --- | --- | --- | --- |
|  |  |  | 是 / 否 |

---

## 审批记录（可选 · 硬门槛）

| 门槛 | 状态 | 日期 | 批准人 |
| --- | --- | --- | --- |
| ① 需求审批 | 待审批 / 已批准 |  |  |
| ② 实现验收 | 待验收 / 已通过 |  |  |

<!-- ========== /可选强化 ========== -->

---

## 进度日志（按需追加）

### YYYY-MM-DD HH:MM

> - <做了什么>
> - <决策 / 关键发现>

---

## 决策记录（按需追加）

### YYYY-MM-DD

> - **决策**：<...>
> - **理由**：<...>
> - **影响**：<...>
> - **已回写**：否 / 是 → `<design/… 路径>`

---

## 引用一致性（归档前必走）

完成时**先**同步更新所有引用，**再**移动文件到 `done/<YYYY-MM>/`：

- [ ] `current-sprint.md` —— 移除该任务索引行
- [ ] `backlog.md` —— 若曾登记，迁移到 `[REJECTED]` 或相应位置
- [ ] `roadmap.md` —— 按需更新进度

> 禁止：未完成引用更新前移动文件。

---

## 关联

> - 相关 task：<task-...>
> - 相关 backlog 项：<...>
