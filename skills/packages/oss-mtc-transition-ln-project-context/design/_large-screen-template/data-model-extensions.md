# 数据模型扩展：（请替换为中文名称）

> **权威**：全局字段与表设计以 [data-models.md](../../design/data-models.md) 为准。本文件只记录 **仅与本特性相关的增量**、**待回填 design 的草稿**，或 **实现阶段确认的附加约束**。

## 1. 本特性涉及的实体 / 表

| 实体或表 | 与 design 关系（引用章节） | 说明 |
|----------|----------------------------|------|
| | | |

## 2. 字段映射

| 图表数据结构字段 | DB 列 | 表 | 转换逻辑 | 说明 |
|-----------------|-------|-----|----------|------|
| | | | | |

## 3. 合并回 design 的约定

- 当本特性相关的库表在 migration 中定稿后，应将字段级描述 **合并** 至 [data-models.md](../../design/data-models.md)，并在本节标注「已合并 / 日期」。
- 跨特性共享的模型变更必须走 design 评审，避免在多个 `data-model-extensions.md` 中重复定义。

## 4. 规则与约束（可选）

（引用或扩展 [data-models.md](../../design/data-models.md) §10 类规则。）
