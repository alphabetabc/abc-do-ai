# Task · 2026-08-14-036-039040信访数据确认

> 状态：✅ 完成
> 类型：数据确认（039/040 信访大屏共性前置）
> 创建：2026-08-14
> 完成：2026-08-17
> 前置：task-030（039 spec 修复完成）+ task-031（040 spec 修复完成）

---

## 0. 任务信息

| 项        | 值                                                                                                |
| --------- | ------------------------------------------------------------------------------------------------- |
| 编号-slug | 2026-08-14-036-039040信访数据确认                                                                |
| 任务类型  | 数据确认                                                                                          |
| 影响范围  | 039 spec §10 O-2/O-3 + 040 spec §10 O-2/O-3 回填                                                   |
| 验收标准  | ① 确认 letter_screen_1~6 表数据状态 ② 确认 §7.4 章节错位是否影响查询 ③ 确认 varchar 字段类型影响 ④ 回填 spec §10 |

---

## 1. 查询结果（2026-08-17 执行）

### 1.1 数据库表数据状态

| 表 | §7 章节 | 是否存在 | 数据行数 | 备注 |
|----|---------|----------|----------|------|
| `letter_screen_1` | §7.1 | ✅ 是 | **0 条** | ⚠️ 空表 |
| `letter_screen_2` | §7.2 | ✅ 是 | **0 条** | ⚠️ 空表 |
| `letter_screen_3` | §7.3 | ✅ 是 | **0 条** | ⚠️ 空表 |
| `letter_screen_4` | 无独立章节 | **❌ 不存在** | — | 🔴 表不存在，039/040 spec 模块4 地图分布引用此表，需修正 |
| `letter_screen_5` | §7.4 | ✅ 是 | **0 条** | ⚠️ 空表；字段结构：ID/CANT_NAME/CANT_CODE/NUM/ORGAN_ID/MAX_TIME/REG_AUTHORITY/STATS_DATE |
| `letter_screen_6` | §7.5 | ✅ 是 | **0 条** | ⚠️ 空表 |

**结论**：
- 6 张表中 5 张存在但全部为空表（0 行数据），需协调数据导入
- `letter_screen_4` **不存在**，是本次查询最关键的发现

### 1.2 O-2：§7.4 章节错位确认 ✅ 已确认

**查询结果**：
- `letter_screen_4` 表在数据库中**不存在**
- `letter_screen_5` 表存在，字段为 `CANT_NAME/CANT_CODE/NUM/ORGAN_ID/MAX_TIME/REG_AUTHORITY/STATS_DATE`
- data-models.md §7.4 标题为 `letter_screen_5`，字段定义与实际表**完全一致**

**结论**：
- data-models.md §7.4 标题 `letter_screen_5` **正确，无错位**
- **实际问题**：039/040 spec 中模块4 地图分布的数据源引用 `letter_screen_4`（不存在的表），应修正为 `letter_screen_5`
- 039 spec 中约 17 处引用 `letter_screen_4`，编码阶段需统一修正

**回填**：039 spec §10 O-2 和 040 spec §10 O-2 已关闭

### 1.3 O-3：varchar 字段类型确认 ✅ 已确认

**查询结果**：

| 表 | varchar 字段数 | 其他类型字段 |
|----|----------------|-------------|
| letter_screen_1 | 6 个（ID, ITEM_NAME, REG_MAIN_APPEAL_ONE, NUM, ORGAN_ID, REG_AUTHORITY） | STATS_DATE: timestamp without time zone |
| letter_screen_2 | 16 个（ID + NUM_0~NUM_11 + ORGAN_ID + ACCEPT_SOURCE + REG_AUTHORITY） | STATS_DATE: timestamp without time zone |
| letter_screen_3 | 15 个（ID + 11 类身份字段 + ORGAN_ID + ACCEPT_SOURCE + REG_AUTHORITY） | STATS_DATE: timestamp without time zone |
| letter_screen_5 | 7 个（ID, CANT_NAME, CANT_CODE, NUM, ORGAN_ID, MAX_TIME, REG_AUTHORITY） | STATS_DATE: timestamp without time zone |
| letter_screen_6 | 11 个（ID + 9 类诉求字段 + ORGAN_ID + REG_AUTHORITY） | STATS_DATE: timestamp without time zone |

**结论**：
- 所有数值字段确实为 `varchar` 类型，与 data-models.md §7 标注**完全一致**
- `STATS_DATE` 为 `timestamp without time zone`（即 datetime），与标注一致
- Service 层数值计算需做 `CAST(... AS DECIMAL/INTEGER)` 转换（已在 plan.md 缓解措施中登记）

**回填**：039 spec §10 O-3 和 040 spec §10 O-3 已关闭

---

## 2. 执行步骤（已完成）

1. ✅ 连接 Kingbase 数据库（10.10.6.142:54321/pdva）
2. ✅ 查询 6 张表的数据状态
3. ✅ 查询 `letter_screen_5` 的表结构（`letter_screen_4` 不存在）
4. ✅ 与 §7.4 字段定义比对，确认 data-models.md 正确
5. ✅ 回填 039 spec §10 O-2/O-3 和 040 spec §10 O-2/O-3

---

## 3. 硬约束

- 不修改 PM 输入原始内容
- 不编造数据
- 查询结果须实际执行 SQL 获得

---

## 4. 关键发现

### 4.1 高优先级：`letter_screen_4` 表不存在

039 spec 中模块4（地图分布）的 SQL 查询、数据源引用、风险评估等均引用 `letter_screen_4`，但该表在数据库中不存在。需要：
1. 确认是否需要创建 `letter_screen_4` 表
2. 若地图分布改用 `letter_screen_5`（地区统计），则需在编码阶段修正 039 spec 中约 17 处引用
3. 同步更新 040 spec（继承自 039）

### 4.2 中优先级：所有表为空表

`letter_screen_1~3,5,6` 共 5 张表均为 0 条数据，大屏开发完成后无法展示。需协调数据导入。

---

## 5. 状态记录

| 日期       | 变更      |
| ---------- | --------- |
| 2026-08-14 | task 创建 |
| 2026-08-17 | 数据库查询完成，O-2/O-3 决议回填 039+040 spec §10；关键发现：letter_screen_4 不存在，全部表为空表 |