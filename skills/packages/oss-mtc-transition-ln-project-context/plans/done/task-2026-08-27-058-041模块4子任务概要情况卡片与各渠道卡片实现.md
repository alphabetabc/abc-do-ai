# Task · 2026-08-27-058-041模块4子任务·概要情况卡片 + 各渠道卡片前端实现

> 状态：🟡 进行中（ChannelComparison 已落地为独立 InfoCard 模块；待闸门 B 验收 + docs 同步）
> 关联：`plans/roadmap-2026-08-11-big-screen.md` §3
> 类型：**task-057 子任务（child task）**——前端卡片组件实现，复用 task-057 3 个端点中的 2 个（/summary + /channels）
> 创建：2026-08-27
> 前置：
>
> - **task-057**（parent）已完成 **3 端点**（/map + /summary + /channels）+ EcMap 地图渲染
> - task-054 公共 Service 已首建
> - 042 共享组件：`InfoCard`(004)、`EcMap`(002)、`RadarChart`(012) 已就绪
> - PM 输入拆分已落地（spec §3.3 模块4 大改）：
>     - 「概要情况」模块（PM 命名）= 3 个单值卡片（合计维度，不做三维度对比）→ 来自 /summary 端点，**位于 MapComparison InfoCard 内顶部**
>     - 「各渠道」模块（PM 命名）= 6 数据点（2 时间 × 3 渠道）→ 来自 /channels 端点，**已重构为独立 InfoCard 模块 `channel/index.tsx`（l 1385 t 108 / 500×406）**，与 MapComparison 平级

---

## 0. 特性信息

| 项              | 值                                                                                                                                                                                                                |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 编号-slug       | 2026-08-27-058-041模块4子任务·概要情况卡片 + 各渠道卡片前端实现                                                                                                                                                   |
| spec 目录       | `docs/specs/041-bigdata-petition-comparison-display/`（复用既有五件套）                                                                                                                                           |
| 关联 spec       | 041（主）                                                                                                                                                                                                         |
| **parent task** | **task-057 模块4 地图全栈开发（3 端点拆分版）**（本 task 为其子任务）                                                                                                                                             |
| 智能体会话      | 一子任务一会话；本 task 独立执行                                                                                                                                                                                  |
| **目标**        | 在 task-057 已落地的 /summary + /channels 端点数据基础上，实现前端「概要情况卡片组」+「各渠道卡片组」组件；**「各渠道」按 2026-08-27 架构重构为独立 InfoCard 模块（`channel/index.tsx`），与 MapComparison 平级** |

---

## 1. 范围

**目标**：把 task-057 /summary 端点返回的 `summary[]` + /channels 端点返回的 `channels[]` 两块数据渲染为前端卡片组件。

**PM 输入拆分落地（2026-08-27 架构调整版）**：

| PM 命名      | 内容                                                     | 数据来源端点                  | 位置                                 | 模块归属                                                     |
| ------------ | -------------------------------------------------------- | ----------------------------- | ------------------------------------ | ------------------------------------------------------------ |
| **概要情况** | 3 个单值卡片（昨日信访量 / 频次 / 人次）                 | `/summary` 端点 `summary[]`   | l 555 t 108 / 矩形 169 上            | **MapComparison 子组件**（InfoCard 内顶部）                  |
| **各渠道**   | 6 数据点（昨天/累计 × 来访/来信/网络，**彻底数据点化**） | `/channels` 端点 `channels[]` | l 1385 t 108 / 500×406 / 矩形 168 上 | **ChannelComparison 独立 InfoCard**（与 MapComparison 平级） |

**交付物**：

| 层               | 交付物                                                                                                                                                                                                                                                                                    |
| ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 前端概要情况组件 | `frontend/src/pages/visual/big-screen/petition-comparison/map/components/SummaryCards.tsx`（task-057 已落地）：3 个单值卡片（昨日信访量 / 频次 / 人次），InfoCard(042-004) 顶部 title 区背景 `gaiyao-bg.png`；`.map(summary.slice(0,3), renderCard)` 数组渲染（已删冗余 ordered useMemo） |
| 前端各渠道模块   | `frontend/src/pages/visual/big-screen/petition-comparison/channel/index.tsx`（**独立 InfoCard 模块，已落地**）：6 数据点 2 行组（行=period 行条 group.png / 列=label 指标格 indicator-bg-1，`groupByPeriod` 动态提取不硬编码）+ `EMPTY_ARRAY` 兜底；每张子卡 `ChannelCard` 含 loading skeleton                      |
| 集成             | `frontend/src/pages/visual/big-screen/petition-comparison/index.tsx`：平级注册 `<ChannelComparison />` + `<MapComparison />`（后者内部嵌入 `<SummaryCards />`）                                                                                                                           |
| 测试             | 3 单值卡片渲染 / 6 数据点栅格渲染 / 加载空错误三态                                                                                                                                                                                                                                        |

**不在范围**：

- /summary 端点实现（task-057 parent 任务）
- /channels 端点实现（task-057 parent 任务）
- /map 端点 + EcMap 地图渲染（task-057 parent 任务）
- 6 模块集成 + InteractionStore 联动（task-060）
- ChannelCards 重新嵌入 MapComparison（原计划已废止，2026-08-27 改为平级模块）

---

## 2. 数据口径速览（详见 041 spec §3.3 模块4 + dme §2.4）

### 2.1 概要情况卡片（来自 /summary 端点）

```typescript
summary: [
    { key: "yesterdayPetition", label: "昨日信访量", value: 1234, unit: "件" },
    { key: "yesterdayBatchFrequency", label: "昨日集体访频次", value: 56, unit: "次" },
    { key: "yesterdayBatchPeople", label: "昨日集体访人次", value: 789, unit: "人次" },
];
```

| 数组项 | 卡片名称       | 数据字段            | 备注          |
| ------ | -------------- | ------------------- | ------------- |
| `[0]`  | 昨日信访量     | `summary[0]` 全字段 | `unit='件'`   |
| `[1]`  | 昨日集体访频次 | `summary[1]` 全字段 | `unit='次'`   |
| `[2]`  | 昨日集体访人次 | `summary[2]` 全字段 | `unit='人次'` |

> **合计维度**（不做合计/本地/进京三维度对比，PM 拍板）
>
> - summary 数组只展示「合计」维度（`local + beijing` 合并值）
> - Service 层派生：`value = local[k] + beijing[k]`（task-057 已实现）
> - **数组化优势**：服务端新增指标（如「昨日敏感件」），summary 数组 append 一项 → 前端 .map 自动渲染，**0 改动**

### 2.2 各渠道卡片（来自 /channels 端点，**彻底数据点化**）

```typescript
channels: [
    // 6 数据点，每个数据点独立一项
    { period: "yesterday", label: "来访", value: 50, unit: "件" },
    { period: "yesterday", label: "来信", value: 30, unit: "件" },
    { period: "yesterday", label: "网络", value: 100, unit: "件" },
    { period: "cumulative", label: "来访", value: 500, unit: "件" },
    { period: "cumulative", label: "来信", value: 300, unit: "件" },
    { period: "cumulative", label: "网络", value: 1000, unit: "件" },
];
```

| 数组项 | 时间维度 | 渠道 | 数据字段             |
| ------ | -------- | ---- | -------------------- |
| `[0]`  | 昨天     | 来访 | `channels[0]` 全字段 |
| `[1]`  | 昨天     | 来信 | `channels[1]` 全字段 |
| `[2]`  | 昨天     | 网络 | `channels[2]` 全字段 |
| `[3]`  | 累计     | 来访 | `channels[3]` 全字段 |
| `[4]`  | 累计     | 来信 | `channels[4]` 全字段 |
| `[5]`  | 累计     | 网络 | `channels[5]` 全字段 |

> 卡片展示形态（**2026-08-28 设计稿对齐**：原 3×2 栅格已改为 **2 行组布局**——group.png 行条 + indicator-bg-1 指标格，详见 §3.2）：
>
> - 2 行组平铺（2 时间 × 3 渠道）：行组 = group.png 行条（「昨天/累计」居中压条）+ 3 张 indicator-bg-1 指标格
> - 行组 = `period`（按数据插入顺序动态提取，yesterday / cumulative）
> - 列 = `label`（按数据插入顺序动态提取，来访 / 来信 / 网络）
> - **数组化优势**：服务端新增时间段或渠道字段（如 `sms`），channels 数组加字段 / 加项 → 前端 `groupByPeriod` 自动适配行组，**0 改动**

### 2.3 复用件（task-057 + task-054 已落地）

- task-057 /summary 端点（含 `summary[]`）+ /channels 端点（含 `channels[]`）
- ahooks `useRequest` 三态
- `utils/empty.ts` 空值统一兜底
- `InfoCard`(042-004) 共享组件

---

## 3. 步骤

### 3.1 前端概要情况组件（SummaryCards · task-057 已落地）

- [x] ✅ `frontend/src/pages/visual/big-screen/petition-comparison/map/components/SummaryCards.tsx` 已建：
    - `InfoCard`(042-004) 包裹，title='辽宁范围信访量概要情况'（**实际落地文案**，与模块4 InfoCard 同名 — SummaryCards 作为 MapComparison 内部子区，无独立 title，由 MapComparison 外壳 title 承载）
    - **`.map(items.slice(0, 3), renderCard)` 数组渲染**：服务端给什么就渲染什么，扩展无需改组件
    - 每张卡片展示：`value（大号字） + unit + label + 进京/本地副指标`（如 `1234 件 / 昨日信访量 / 进京: x 本地: y`）
    - 卡片样式：背景 `gaiyao-bg-205-78.png`（205×78 专用资源，运行时 `resolvePublicAssetUrl` 注入，避免破坏 Vite 打包），3 列水平排列
    - 子指标（2026-08-28 定稿）：进京/本地**单行水平并排**（columnGap 22 居中），Source Han Sans CN 14px/21px，进京 `rgba(0,252,255,1)` / 本地 `rgba(247,181,1,1)`，内容块 `marginLeft: 65`
- [x] ✅ 数据 props：组件自带 `useRequest(getSummary)` 拉取（不与 parent 共享，节省耦合；未来 organId 联动由 task-060 接入 InteractionStore 时统一改）
- [x] ✅ 加载态：每张卡片显示 skeleton 占位（**2026-08-28 骨架修复**：空数据时 map 零渲染致骨架不出 → `displayItems` 三元分支以 `[null,null,null]` 占位渲染整组骨架）
- [ ] ⏸️ 空/错误态：`utils/empty.ts` 兜底已就位（`EMPTY_ARRAY`），但当前 `channel/index.tsx`/`SummaryCards` 未显式渲染 `Empty`/`message.error` 错误提示分支 — **顺延至 task-060 联调集成期统一补齐**（用户 2026-08-28 拍板编码收口）

### 3.2 前端各渠道模块（**ChannelComparison 独立 InfoCard · 2026-08-27 架构重构版**）

- [x] ✅ `frontend/src/pages/visual/big-screen/petition-comparison/channel/index.tsx`（**独立 InfoCard 模块，已落地**）：
    - `InfoCard`(042-004) 包裹，title='辽宁范围各渠道信访量'（**实际落地文案**，少"进京"二字——与 task-058 §1 PM 命名存在差异，待 PM 定夺）
    - 布局参数（**已实参**）：`width:500 / height:406 / left:1385 / top:108 / titleBg normal / contentHeight:346`
    - **行组布局（2026-08-28 设计稿对齐，替代原 3×2 栅格）**：2 个 `PeriodGroup` 行组，每组 = 行条 + 3 张指标格
        - 行条：`group.png`（457×29）背景，period 文案「昨天/累计」16px/700 白字居中压条
        - 指标格：`indicator-bg-1.png`（148×121）背景 ×3，格内 = 渐变数值 + 渠道名 + 进京/本地副指标
        - `groupByPeriod(items)`：**数组化核心**，按数据插入顺序动态提取 period × label 交叉分组，扩展无需改组件
    - `ChannelCard` 子组件：每张格展示 `value（渐变大号字）+ unit + label + 进京/本地副指标`（如 `100 件 / 网络 / 进京: x 本地: y`）
    - 数值渐变（2026-08-28）：`VALUE_STYLE` = YouSheBiaoTiHei 26.85px + `linear-gradient(0deg, rgba(117,207,255,1) 32.766% → rgba(255,255,255,1) 79.841%)` + `backgroundClip: text`
    - **数组化优势**：服务端新增时间段或渠道字段 → `groupByPeriod` 自动适配行组 → **0 改动**
- [x] ✅ 数据拉取：模块自带 `useRequest(getBigScreenPetitionComparisonChannels)`（与 MapComparison 的 `useRequest(getMap)` 独立，不共享）
- [x] ✅ 加载态：每张卡片显示 skeleton 占位（`SKELETON_VALUE` + `SKELETON_LABEL`；**2026-08-28 骨架修复**：空数据时 `groupByPeriod([])` 零行组致骨架不出 → `SKELETON_GROUPS` 占位 + `displayGroups` 三元分支渲染 2×3 完整骨架；`SKELETON_VALUE` 重置 `backgroundClip: 'border-box'` 防渐变 clip 裁剪致骨架不可见）
- [ ] ⏸️ 空/错误态：`EMPTY_ARRAY` 兜底已就位，但当前未显式渲染 `Empty`/`message.error` 错误提示分支 — **顺延至 task-060 联调集成期统一补齐**（用户 2026-08-28 拍板编码收口）

### 3.3 大屏页面集成（`petition-comparison/index.tsx`，**2026-08-27 架构重构版**）

- [x] ✅ **ChannelComparison 已平级注册**（任务主线已落地）：
    - `frontend/src/pages/visual/big-screen/petition-comparison/index.tsx` 改为**平级渲染 5 模块**：`TrendComparison` / `AppealComparison` / `ChannelComparison` / `MapComparison` / `RankComparison`
    - **不再**在 MapComparison InfoCard 内嵌入 `<ChannelCards />`（原 task-058 主任务"ChannelCards 接入 MapComparison"**已废止**，因架构重构为平级模块）
- [x] ✅ SummaryCards 仍嵌入 MapComparison InfoCard 内顶部（task-057 完成）：
    - `<SummaryCards />` 在 `map/index.tsx` InfoCard 内顶部 65px（绝对定位 top:0）
    - SummaryCards 自带 useRequest，与 MapComparison 的 `useRequest(getMap)` 共存不冲突
- [x] ✅ EcMap 已接入 MapComparison（task-057 完成）：
    - `<EChartsMap />` 在 MapComparison InfoCard 内中部（绝对定位）
    - `mapData` 来自 MapComparison 自带 `useRequest(getBigScreenPetitionComparisonMap)`
- [x] ✅ 布局（**已同步至实参**）：
    - MapComparison InfoCard（w 810 / h 651 / l 555 / t 108 / titleBg normal / contentHeight 600）
        - SummaryCards：`position:absolute, top:0, left:0, width:100%, height:65, zIndex:10`（已接入）
        - EcMap：`position:absolute, left:50%, top:52%, transform:translate(-50%,-50%)`，容器 `width:717 / height:517`（已接入）
    - ChannelComparison InfoCard（w 500 / h 406 / l 1385 / t 108 / titleBg normal / contentHeight 346）
        - 3×2 栅格（`gridTemplateColumns: 'repeat(3, 1fr)'` + `gridTemplateRows: 'repeat(2, 1fr)'`）

### 3.4 presets.ts 拆分（**已完成 + 调整**）

- [x] ✅ `map/presets.ts` 当前内容（task-057 完成）：
    - `buildMapData(regions)`：map 端点 regions → MapChartItem 转换
    - `computeVisualMapMax(items)`：visualMap max 动态绑定
    - `MAP_VISUAL_MAP`：visualMap 配色常量
    - `mapSeriesBuilder(config)`：**从 index.tsx 抽离的 EcMap series 配置**（用户 2026-08-27 反馈移入 presets.ts，函数不依赖组件 state）
- [x] ✅ **删除预设转换函数**：原计划 `summaryToCards` / `channelsToCards` **不再需要**——数组化后组件直接 `.map` 渲染，无需中间转换
- [x] ✅ **保留空值过滤**：由 `EMPTY_ARRAY`（@/utils/empty）统一兜底
- [x] ✅ **ChannelComparison 无独立 presets**（简单 `orderChannels` 内联足够，无需独立文件）

### 3.5 联调 + 收尾

- [ ] ⏸️ 前后端联调对数：Service 输出与 DB 直查聚合一致（task-057 已落地的 3 端点；task-058 共享，无新增端点）— **顺延至 task-060 联调集成期实库补对**
- [x] ✅ 更新 041 tasks.md：勾选本 task 完成条目 + 模块4-3 路径描述修正（`channel/index.tsx` + `channel/components/ChannelCard.tsx`，task-058 单卡拆分后路径）—— 2026-08-28 收尾执行
- [x] ✅ roadmap §3 状态流转：本 task ✅ 完成 → `done/`（2026-08-28 用户拍板编码收口，随 parent task-057 一并流转）

### 3.6 闸门 B（收尾验收）

- [x] ✅ 渲染验收：2 行组（`PeriodGroup` × `groupByPeriod` 动态适配）+ 3 单值卡片渲染（slice(0,3)）已实现，`tsc --noEmit` 0 错误（验收以 2026-08-28 行组布局为准，原 3×2 Grid 已重构）
- [ ] ⏸️ 加载/空/错误三态：加载态 skeleton ✅（`SKELETON_GROUPS` + `displayItems` 占位分支）；空态 `Empty` + 错误态 `message.error` — **顺延至 task-060 联调集成期**（用户 2026-08-28 拍板编码收口）
- [x] ✅ docs 同步自查：`Grep ".trae|agents.md|.local-" docs/specs/041-*` **0 命中**（2026-08-28 验证）
- [x] ✅ 复用件确认：`InfoCard`(042-004) + task-057 `/summary` + `/channels` 端点 + task-054 公共 Service + `EMPTY_ARRAY` 兜底 — 均按约定复用，未重复实现
- [ ] ⏸️ 命名对齐：ChannelComparison title 是否补"进京"二字 — **遗留待 PM 拍板**（当前落地文案「辽宁范围各渠道信访量」）

---

## 4. 上下游依赖关系

### 4.1 上游（依赖本 task 之前完成）

- **task-057**（parent，**3 端点拆分版**）：
    - /summary 端点（含 `summary[]`）+ /channels 端点（含 `channels[]`）+ /map 端点（含 `regions`）
    - map/index.tsx 骨架（含 SummaryCards 子组件 + EcMap 集成）
    - frontend API 封装：`getBigScreenPetitionComparisonMap` / `Summary` / `Channels`
- **task-054**：041 公共 Service 首建
- 042 共享组件：`InfoCard`(004)
- 架构前置（2026-08-27 调整）：petition-comparison 5 模块平级结构（Trend / Appeal / Channel / Map / Rank）

### 4.2 下游（本 task 完成后可启动）

- **task-060**（集成收口）：包含 SummaryCards + ChannelComparison 接入 InteractionStore organId 联动

### 4.3 横向依赖

- 无（独立子任务）

---

## 5. Prompt（复制即用）

```text
【会话启动】加载 skill oss-mtc-transition-ln-project-context；
Read @.trae/skills/oss-mtc-transition-ln-project-context/plans/task-2026-08-27-058-041模块4子任务概要情况卡片与各渠道卡片实现.md 了解任务全景（**2026-08-27 架构重构版：ChannelComparison 独立 InfoCard 模块，不再嵌入 MapComparison**）。

【任务】作为 task-057 的子任务，实现「概要情况卡片组」+「各渠道卡片组」前端组件，共享 task-057 /summary + /channels 端点数据，范围以 task 文件 §1 为准。

必读：
- parent task（3 端点拆分版）：@.trae/skills/oss-mtc-transition-ln-project-context/plans/task-2026-08-27-057-041模块4地图全栈开发.md
- 复用：@.trae/skills/oss-mtc-transition-ln-project-context/plans/done/task-2026-08-27-054-041模块2诉求占比对比全栈开发.md
- 组件契约：@docs/specs/042-components-common/004-info-card/spec.md
- spec.md §3.3 模块4 + data-model-extensions.md §2.4

硬约束：
1. **共享 task-057 端点数据**（不重新调用 API）；task-057 已建 3 个端点封装：`getBigScreenPetitionComparisonMap` / `Summary` / `Channels`。
2. **summary 卡片只展示合计维度**（服务端已聚合 `value = local + beijing`，前端无三维度对比）。
3. **架构：SummaryCards 嵌入 MapComparison；ChannelComparison 独立 InfoCard 模块**（2026-08-27 重构，与 MapComparison 平级）。
4. **数组化设计原则**：服务端给什么就渲染什么，扩展无需改组件。
   - summary 数组每项 `{key, label, value, unit}`，组件 `.map(items.slice(0,3), renderCard)` 渲染
   - channels 数组每项 `{period, label, value, unit}`，组件 `orderChannels` 按 (period × label) 交叉索引 → 3×2 栅格
5. 加载/空/错误三态完整；utils/empty.ts 统一兜底。当前 loading skeleton ✅；空态 `Empty` + 错误态 `message.error` 待补。
6. 前端布局参数（**已同步至实参**）：
   - MapComparison InfoCard：宽 810px / 高 651px / l 555 / t 108（titleBg normal / contentHeight 600）
   - MapComparison 内：SummaryCards `top:0 / height:65`（绝对定位，task-057 完成）+ EcMap 中部
   - ChannelComparison InfoCard：宽 500px / 高 406px / l 1385 / t 108（titleBg normal / contentHeight 346）
7. 不得新建独立 API；/summary + /channels 端点形状由 task-057 拍板（不臆造）。
8. 每完成一个切片在 task 文件 §3 勾选并同步 041 tasks.md。

完成后列出：改动文件清单、勾选状态、遗留开放问题。
```

---

## 6. 状态记录

| 日期       | 变更                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-08-27 | Task 创建（⚪ 待启动）；定位为 task-057 的**子任务**——前端卡片组件实现（不重复实现 /map 端点）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-27 | 修订：parent task-057 改为 **3 端点拆分版**（/map + /summary + /channels）；本 task 共享 /summary + /channels 端点数据，仍负责前端 SummaryCards + ChannelCards 组件实现                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 2026-08-27 | 修订：channels 数组彻底数据点化（每数据点独立一项，共 6 项 = 2 时间 × 3 渠道）—— 与 summary 形态对齐                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 2026-08-27 | **任务进入 → 🟡 进行中**：task-057 已落地 SummaryCards.tsx + ChannelCards.tsx 占位实现（数据接入 + 基础布局）+ map/index.tsx 接入 SummaryCards + EcMap；map/presets.ts 已含地图相关 4 函数（**`mapSeriesBuilder` 从 index.tsx 抽离**）+ 删除 `summaryToCards` / `channelsToCards`（数组化无需转换）；**本 task 待接力**：① ChannelCards 接入 MapComparison（用户 2026-08-27 反馈当前未引入）② 卡片动画/空态完整化 ③ organId 跨模块联动（task-060 集成时）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2026-08-27 | **SummaryCards 视觉接力（部分完成）**：卡片背景 gaiyao-bg.png（195×68）通过运行时 `resolvePublicAssetUrl('/static/images/gaiyao-bg.png')` 注入（不用 `import` 引入 public 资源，避免破坏 Vite 打包）；删除冗余 `ordered useMemo`（后端已固定顺序）+ 删除 `useMemo` import + 删除 `as any` 类型断言；改为 `items.slice(0, 3).map(...)` 数组渲染                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 2026-08-27 | **ChannelCards 占位实现（已落地）**：3×2 栅格（grid repeat(3,1fr) / repeat(2,1fr)）+ `orderChannels(items)` 按 (period × label) 交叉索引 + `useMemo` 缓存；label 显示为 `{period ? '昨日':'累计'} · {item.label}`；卡片样式 rgba(11,64,117,0.4) 背景 + 边框（**未用 gaiyao-bg**，与 SummaryCards 视觉区分）；当前 MapComparison 未引入，待本 task 接入                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 2026-08-27 | **架构重构 + 文档同步**：ChannelCards **不再**嵌入 MapComparison（用户 2026-08-27 拍板），改为**独立 InfoCard 模块 `petition-comparison/channel/index.tsx`**，与 MapComparison 平级；`petition-comparison/index.tsx` 改为平级注册 5 模块（Trend / Appeal / Channel / Map / Rank）；布局参数 w 500 / h 406 / l 1385 / t 108 / titleBg normal / contentHeight 346。**task 文件同步更新**：① line 3 状态 ⚪ → 🟡（与 §6 历史一致）② §1 PM 输入拆分表新增"模块归属"列 ③ §1 交付物 ChannelCards 行迁移到 `channel/index.tsx` ④ §2.2 卡片展示形态改为 3×2 栅格（原"2 大卡片"作废）⑤ §3.2 重写 ChannelCards 计划为独立模块版 ⑥ §3.3 重写集成计划，原"ChannelCards 接入 MapComparison"主任务**废止** ⑦ §4 上下游依赖关系更新 ⑧ §5 Prompt 硬约束更新 ⑨ §3.6 闸门 B 新增"命名对齐：title 是否补'进京'二字"开放项。**遗留**：① 空态 `Empty` + 错误态 `message.error` 回调待补 ② organId 跨模块联动由 task-060 接入 ③ ChannelComparison title 命名待 PM 拍板 ④ 041 tasks.md 勾选 + roadmap §3 状态流转待执行 |
| 2026-08-27 | **三维扩展（指标卡片进京/本地细分）落行**：在 `/summary` + `/channels` 端点的每项 `{ key, label, value, unit }` 基础上扩展 `local` + `beijing` 两字段，**不变量 `value == local + beijing`**；Service 层沿用 §7.44 双查询（`RA=2` 本地 + `RA=1` 进京）分别累加。**后端**：① `backend/app/schemas/visual.py` `BigScreenPetitionComparisonSummaryItem` + `BigScreenPetitionComparisonChannelItem` 新增 `local: int` / `beijing: int = 0` 字段（向后兼容 additive）② `backend/app/services/visual/big_screen/petition_comparison.py` `_aggregate_summary_rows` + `_aggregate_channels_rows` 输出三维度 ③ tests `test_big_screen_petition_comparison_summary` + `test_big_screen_petition_comparison_channels` 新增 local/beijing 断言 + 不变量校验（**20 用例全过**）。**前端**：① `frontend/src/services/visual-big-screen-petition-comparison.ts` `BigScreenPetitionComparisonSummaryItem` + `BigScreenPetitionComparisonChannelItem` 加 `local: number` / `beijing: number` ② `map/components/SummaryCards.tsx` 主数字下方新增 2 行堆叠「进京（玫红 rgba(254,84,120,1)）/ 本地（蓝 rgba(0,145,255,1)）」配色与 trend 模块一致；卡片高度 68→92、容器 65→95 以容纳 2 行 breakdown；新增 `BREAKDOWN_*_STYLE` 常量 ③ `channel/index.tsx` `ChannelCard` 同款 breakdown 样式 ④ `map/index.tsx` 容器高度 65→95 + header 注释更新。**L3 docs 同步**：① `docs/design/api-contracts.md` §7.44 `/summary` + `/channels` Service 派生公式三维度（Python 公式 + 字段含义）② `docs/specs/041-bigdata-petition-comparison-display/spec.md` §3.2 端点表 + §3.3 模块4 组件映射（2026-08-27 三维扩展 note）③ `docs/specs/041-bigdata-petition-comparison-display/data-model-extensions.md` §2.4.2 summary 表 + §2.4.3 channels 表新增 local/beijing 列（2026-08-27 三维扩展 note + 向后兼容）④ `plans/roadmap-2026-08-11-big-screen.md` §4 新提案 `R-DOCS-API-CONTRACTS-041-LB-EXT` [x] 已执行 + §5 变更记录 2026-08-27 行追加。**特性：① 卡片下方 2 行堆叠，进京=玫红 / 本地=蓝（配色与 trend 模块一致）② 数组化保留（summary 数组 append 指标 + channels 数组 append 数据点均自动适配栅格）③ 向后兼容（additive 扩展，不影响已接入模块）** |
| 2026-08-27 | **SummaryCards 卡片尺寸修正（PM 指定 205×78）**：`CARD_BASE_STYLE` 尺寸 195×92 → **205×78**；`gap: 2 → 0`（内容高度 76px = value 24 + label 20 + 2×breakdown 16，无 gap 恰好落入 78px 卡内，`justifyContent: center` 上下各留 1px）；`map/index.tsx` SummaryCards 容器高度 95 → **78**（对齐卡片尺寸，注释同步）；背景图 `gaiyao-bg.png` 经 `backgroundSize: '100% 100%'` 拉伸铺满，无需替换资源 |
| 2026-08-28 | **设计稿视觉对齐 + 骨架屏修复（本轮会话，实现主线收口）**：① **ChannelComparison 渲染结构重构**（设计稿对齐）：原 3×2 栅格（`orderChannels`）→ **2 个 `PeriodGroup` 行组**（`groupByPeriod` 动态分组）；行条 `group.png`（457×29）背景 +「昨天/累计」16px/700 白字居中压条（原「昨日」改「昨天」）；指标格 `indicator-bg-1.png`（148×121）×3；数值改渐变文字 `VALUE_STYLE`（YouSheBiaoTiHei 26.85px + linear-gradient(0deg, rgba(117,207,255,1) 32.766%→rgba(255,255,255,1) 79.841%) + backgroundClip:text）；用户自调布局（CARD_BASE_STYLE 纯块级去 flex/padding/gap、数值区 marginLeft 60/mt 20、副指标区 mt 20/两行 ml 50/本地行 mt 12）；② **SummaryCards 定稿**：背景换 `gaiyao-bg-205-78.png`（205×78 专用资源，替代 gaiyao-bg.png 拉伸方案）；子指标单行水平并排（columnGap 22 居中）、Source Han Sans CN 14px/21px、进京 rgba(0,252,255,1) / 本地 rgba(247,181,1,1)、内容块 marginLeft 65；③ **骨架屏修复（两组件）**：空数据时 map 零渲染致骨架不出——SummaryCards 加 `displayItems` 三元分支（`[null,null,null]` 占位）、ChannelComparison 加 `SKELETON_GROUPS` + `displayGroups` 三元分支（2×3 完整骨架）；`SKELETON_VALUE` 重置 `backgroundClip:'border-box'`（渐变 clip 会裁掉无文字骨架块的背景）。**§1/§2.2/§3.1/§3.2 描述已同步为行组布局**。遗留：大数值 nowrap 溢出风险（累计口径 6~7 位）、空/错误态 + 041 tasks.md 勾选 + roadmap §3 流转待闸门 B |
| 2026-08-28 | **单卡组件拆分（用户指定）**：`SummaryCard.tsx`（`map/components/`，与 SummaryCards 同级）与 `ChannelCard.tsx`（`channel/components/`，新建子目录）各自独立文件（样式常量 + props 接口随组件迁移）；父文件精简为 数据获取 + 行组/布局编排，`tsc --noEmit` 0 错误 |
| 2026-08-28 | **任务完成 → ✅（用户拍板编码收口，随 parent task-057 一并归档）**：line 3 状态 🟡 → ✅；闸门 B 勾选（渲染验收 tsc 0 错误 / docs 自查 0 命中 / 复用件确认）；§3.1/§3.2 空/错误态 + §3.5 联调对数标注顺延 task-060 联调集成期；命名对齐（title 补"进京"）遗留待 PM；041 tasks.md 模块4-2/4-3 条目勾选 + 模块4-3 路径描述修正；roadmap §3 索引 ✅ 完成 → done/。文件移至 `plans/done/` |
