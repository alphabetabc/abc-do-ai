# Task · 2026-08-14-035-038数据依赖确认

> 状态：✅ 完成
> 类型：数据确认 + PM 沟通（038 人员信息大屏前置）
> 创建：2026-08-14
> 完成：2026-08-17
> 前置：task-033（038 spec 修复完成）

---

## 0. 任务信息

| 项        | 值                                                                                                                        |
| --------- | ------------------------------------------------------------------------------------------------------------------------- |
| 编号-slug | 2026-08-14-035-038数据依赖确认                                                                                            |
| 任务类型  | 数据确认 + PM 沟通                                                                                                        |
| 影响范围  | 无文档修改（确认结果回填 038 spec §10）                                                                                   |
| 验收标准  | ① 确认 3 张表数据状态 ② rylb 枚举值确认（O-5） ③ 安置地存储格式确认（O-6） ④ lxgb 字段取值 PM 确认（O-1） ⑤ 回填 spec §10 |

---

## 1. 待确认项

### 1.1 数据库表数据状态

| 表                              | schema        | spec 引用 | 确认内容                                              |
| ------------------------------- | ------------- | --------- | ----------------------------------------------------- |
| `stats_jdlk_persion_infor_cant` | `dw_basic_lc` | §6.4      | 是否有数据、数据量级                                  |
| `abi_zb_tyjrjqyfdx`             | `dw_basic_lc` | §8.2      | 是否有数据、数据量级                                  |
| `aa_jycy`                       | `dw_basic_lc` | dme §2.6  | 是否有数据（task-015 记录为空）、如有数据则确认字段值 |

**注意**：task-015 记录 `aa_jycy` 表建表成功但无数据。需确认是否已导入数据，或协调导入测试数据。

### 1.2 O-5：`rylb` 枚举值确认

- **当前状态**：开放
- **确认方式**：`aa_jycy` 表有数据后，查询 `SELECT DISTINCT rylb FROM dw_basic_lc.aa_jycy` 获取实际枚举值
- **回填**：将枚举值列表写入 038 spec §10 O-5 决议列

### 1.3 O-6：安置地存储格式确认

- **当前状态**：开放
- **确认方式**：`aa_jycy` 表有数据后，查询 `SELECT 安置地 FROM dw_basic_lc.aa_jycy LIMIT 10` 确认是中文地名还是编码
- **回填**：将存储格式（中文/编码）写入 038 spec §10 O-6 决议列

### 1.4 O-1：`lxgb` 字段取值 PM 确认

- **当前状态**：开放
- **确认方式**：向 PM 提交问题——`lxgb` 字段的取值逻辑是什么？是否需要前端转换？
- **回填**：将 PM 确认结果写入 038 spec §10 O-1 决议列

---

## 2. 查询结果（2026-08-17 执行）

### 2.1 数据库表数据状态

| 表                              | schema        | 是否存在 | 数据行数  | 备注                                                |
| ------------------------------- | ------------- | -------- | --------- | --------------------------------------------------- |
| `stats_jdlk_persion_infor_cant` | `dw_basic_lc` | ✅ 是    | **0 条**  | ⚠️ 空表；模块3/4 地图区域无数据可用，需协调数据导入 |
| `abi_zb_tyjrjqyfdx`             | `dw_basic_lc` | ✅ 是    | **60 条** | ✅ 有数据，可用于模块5 优抚对象查询                 |
| `aa_jycy`                       | `dw_basic_lc` | ✅ 是    | **10 条** | ⚠️ 仅有少量测试数据；rylb/安置地 确认基于这 10 条   |

**结论**：

- `stats_jdlk_persion_infor_cant` 空表是**高优先级风险**：模块1/2/3/4 均依赖此表，无数据将导致大屏无法展示。需协调数据导入。
- `aa_jycy` 有 10 条测试数据，可用于字段格式确认，但实际枚举值可能不全。

### 2.2 O-5：`rylb` 枚举值确认 ✅ 已确认

| 枚举值               | 说明                                |
| -------------------- | ----------------------------------- |
| `自主就业退役义务兵` | 当前 10 条数据中仅出现 1 种 rylb 值 |

**注意**：当前仅 10 条数据，rylb 枚举值可能不全。实际生产数据中可能包含更多人员类别（如：自主就业退役士官、安排工作退役士兵、退休军人等）。建议数据导入后重新查询确认完整枚举列表。

**spec §10 O-5 决议建议**：rylb 枚举值待数据全量导入后二次核对；当前以 `SELECT DISTINCT rylb FROM dw_basic_lc.aa_jycy` 结果为准。

### 2.3 O-6：安置地存储格式确认 ✅ 已确认

**存储格式**：**中文地名**（非编码）

样本数据（10 条）：

- `辽宁省锦州市凌河区`
- `辽宁省葫芦岛市兴城市`
- `辽宁省沈阳市新民市`
- `辽宁省阜新市细河区四合镇`
- `辽宁省鞍山市立山区深北街道`
- `辽宁省铁岭市银州区柴河街道`
- `辽宁省葫芦岛市连山区`
- `辽宁省沈阳市皇姑区`
- `辽宁省沈阳市沈河区滨河街道`
- `辽宁省大连市庄河市明阳街道`

**格式特征**：

- 存储为中文地址文本，格式不完全统一（有的到区、有的到镇、有的到街道）
- 可使用 `LIKE '辽宁省XX市YY区%'` 模式做地市级筛选
- 无独立的行政区编码字段

**spec §10 O-6 决议建议**：安置地为中文地名存储；地市级筛选需用 `LIKE concat(cantCodePrefix, '%')` 或 `安置地 LIKE '%市%区%'` 模式匹配。

### 2.4 O-1：`lxgb` 字段取值 ⚠️ 待 PM 确认

**数据库查询结果**：

| lxgb 值 | 条数 | lxgb 值 | 条数 |
| ------- | ---- | ------- | ---- |
| 0       | 8    | 14      | 2    |
| 1       | 2    | 16      | 4    |
| 2       | 2    | 18      | 2    |
| 3       | 6    | 19      | 2    |
| 4       | 6    | 22      | 2    |
| 6       | 2    | 99      | 2    |
| 7       | 2    | 110     | 2    |
| 8       | 2    | 115     | 2    |
| 9       | 4    | 128     | 2    |
| 10      | 2    |         |      |
| 11      | 2    |         |      |
| 13      | 2    |         |      |

**分析**：

- `lxgb` 字段为整数类型（int），取值范围 0~128，共 21 种不同值
- 0 值出现 8 次（可能表示"无/不适用"），非 0 值表示不同级别的离休干部
- **关键发现**：`lxgb` 不是简单的"离休干部人数"单值，而是**分级编码**（不同数值代表不同级别/类型的离休干部）
- PM 原文 `SELECT lxgb FROM abi_zb_tyjrjqyfdx WHERE cant_code = #{cantCode}` 查询单列值，但实际 lxgb 有多级含义

**需向 PM 确认的问题**：

1. `lxgb` 字段的取值逻辑是什么？各数值（0, 1-128）分别代表什么含义？
2. 「优抚对象总数」是否应该 = 所有非 0 的 lxgb 之和？还是取某个特定值？
3. 是否需要前端做数值→文案的转换映射？

**spec §10 O-1 决议建议**：待 PM 回复后回填。

---

## 3. 执行步骤（已完成）

1. ✅ 连接 Kingbase 数据库（10.10.6.142:54321/pdva），查询 3 张表的数据状态
2. ✅ `aa_jycy` 表有 10 条数据，非空（但数据量少，仅测试数据）
3. ✅ 执行 O-5/O-6 的查询，获取 rylb 枚举值和安置地格式
4. ⏳ 向 PM 提交 O-1 问题（lxgb 取值逻辑）
5. ⏳ 汇总确认结果，回填 038 spec §10（需 L3 提案审批）

---

## 4. 硬约束

- 不修改 PM 输入原始内容
- 不编造数据（如表为空则如实记录）
- 查询结果须实际执行 SQL 获得，不得推测

---

## 5. 状态记录

| 日期       | 变更                                                             |
| ---------- | ---------------------------------------------------------------- |
| 2026-08-14 | task 创建                                                        |
| 2026-08-17 | 数据库查询完成，获得 3 表数据状态 + O-5/O-6 结果；O-1 待 PM 确认 |
