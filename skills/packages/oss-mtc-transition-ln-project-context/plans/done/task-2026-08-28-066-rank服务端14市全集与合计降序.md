# Task · 2026-08-28-066-rank服务端14市全集与合计降序

> 状态：✅ 已完成（2026-08-28）
> 类型：编码（backend rank 口径 + frontend presets）+ L3 文档落行
> 创建：2026-08-28
> 背景：用户发现模块5「各地区信访件数量」分组柱图无排名效果——排查结论：PM 排序规则存在（spec §3.3「各按 NUM DESC」）、后端已实现（rank.sql `ORDER BY NUM DESC`），但前端 `rank/presets.ts` 用 `RANK_CITY_ORDER` 14 市固定行政区划清单重排 x 轴，丢弃了后端顺序；根因 = 分组柱图单 x 轴无法承载两组独立排序，实现时前端自建骨架清单（注释错误引用 spec §3.3「沈阳/大连/…」，spec 实无此清单）
> ⚠️ 2026-08-28 用户拍板：①「前端不应该有这个固定顺序，这就是服务端没有处理好，我们看看如何在服务端完成这个处理」② 排序键 = **按合计（local+beijing）降序**（AskUserQuestion 推荐）
> 关联依据：`docs/specs/041-bigdata-petition-comparison-display/spec.md` §3.3/§4.5.5；`docs/design/api-contracts.md` §7.44；`docs/specs/041-bigdata-petition-comparison-display/data-model-extensions.md` §2.5；L3 提案 `R-DOCS-API-CONTRACTS-041-RANK-ORDER`（roadmap §4）

---

## 0. 任务信息

| 项 | 值 |
| --- | --- |
| 编号-slug | 2026-08-28-066-rank服务端14市全集与合计降序 |
| 任务类型 | 编码（backend Service + tests、frontend presets）+ L3 文档同步 |
| 影响范围 | `backend/app/services/visual/big_screen/petition_comparison.py`（rank 段）+ `backend/tests/test_big_screen_petition_comparison_rank.py` + `frontend/src/pages/visual/big-screen/petition-comparison/rank/presets.ts` + `frontend/src/pages/visual/big-screen/petition-comparison/map/presets.ts`（清单本地化）+ L3 文档 4 处 |
| 验收标准 | ① Service：local/beijing 各组固定 14 行全集（缺市补 num=0/maxTime=""），两组**同序**按合计降序（tiebreaker 区划码升序），沈抚合并后参与排序；② 空态契约不变：两组全 0 → `regions: []`；③ 后端 rank 测试更新全过（全集/补 0/同序/沈抚/空态）；④ 前端删除 `RANK_CITY_ORDER` 骨架，`buildChartData` 直接以 local.regions 顺序渲染；map/presets.ts 过滤清单本地化；tsc/eslint 通过；⑤ L3 文档落行（提案已会签） |

---

## 1. 方案（2026-08-28 用户拍板）

- **后端**：`_MOCK_RANK_CITIES` 提升为正式常量 `_RANK_CITIES`（14 市全集）；新增全集补 0 + 两组同序排序（合计降序）；mock 数值调整（阜新进京放大）使合计排序 ≠ 区划序，真正覆盖排序断言
- **前端**：`rank/presets.ts` 删 `RANK_CITY_ORDER` + 骨架遍历，`buildChartData` 直接用 `resp.local.regions` 作 x 轴（beijing 按 cantName/cantCode 容错对齐）；`map/presets.ts` 的 `CITY_NAME_SET` 过滤用途改本地定义清单
- **文档 L3**：spec §3.3 /rank 口径 + §4.5.5 + api-contracts §7.44 /rank 详述 + dme §2.5 响应语义同步

## 2. 步骤

### 步骤 1：roadmap §4 提案登记（R-DOCS-API-CONTRACTS-041-RANK-ORDER）✅ 已完成（2026-08-28，用户拍板即会签，[x]）

### 步骤 2：后端 Service + mock + 测试 ✅ 已完成（2026-08-28）

- `petition_comparison.py` rank 段重构：`_MOCK_RANK_CITIES` → `_RANK_CITIES`（辽宁 14 地市行政区划清单，/rank 全集补 0 权威清单 + 兼作 mock 数据源）；新增 `_fill_rank_city_full_set`（按 6 位市码前缀 / 市名匹配 → 行名称/编码规范化为清单标准值，兼容 6/12 位区划码；缺市补 `{num: 0, max_time: ""}`；未匹配行丢弃）；新增 `_build_rank_response`（两组各自：转 dict → 沈抚合并 → 14 市全集补 0 → **两组同序**按合计（local+beijing）降序、tiebreaker 市码升序；双组全 0 → 双组 `regions: []`）；删除 `_sort_rank_regions_desc` + `_build_rank_block`
- mock：阜新市 beijing `(580, 72)` → `(580, 3000)`（合计 3580 全省第一，区划序第 9，使 mock 可验证「合计降序」≠「区划序」）
- `rank.sql` 保留 `ORDER BY NUM DESC` 组内初排（未改）；`_merge_shenfu_regions` / `_rows_to_rank_regions` 未改
- 测试重写：`test_big_screen_petition_comparison_rank.py` 11 → 16 用例（新增全集规范化补 0 / 两组同序 / tiebreaker / 沈抚参与排序 / mock 阜新第一 / 空态 6 个）

### 步骤 3：前端 presets 改造 ✅ 已完成（2026-08-28）

- `rank/presets.ts`：删 `RANK_CITY_ORDER` 14 市清单 + 骨架遍历；`buildChartData` 重写——x 轴直接采用 `local.regions` 顺序（前端不持有固定地市清单），beijing 按名称 / 6 位编码容错对齐取值（`buildLookup` 保留）
- `map/presets.ts`：删 `import { RANK_CITY_ORDER }`；过滤清单本地化为 `CITY_NAMES` / `CITY_NAME_SET`（辽宁 14 地市名称集合，区划序）
- Grep 确认 `RANK_CITY_ORDER` 全前端仅 2 文件引用，删除后无残留

### 步骤 4：验证（pytest rank + tsc/eslint）✅ 已完成（2026-08-28）

- 后端：`backend\.venv\Scripts\python.exe -m pytest tests/test_big_screen_petition_comparison_rank.py` → **13 passed / 3 failed**；3 failed = 200/400/403 合同测试，根因 `login_with_captcha` 无 `data` → KeyError（基线问题 INVALID_PASSWORD_ENCRYPT，task-065 已记录，与本 task 无关）；13 个 Service 单测（含新增 6 个）全过
- 前端：`node_modules\.bin\tsc.cmd --noEmit` exit 0 + `eslint.cmd`（rank/presets.ts + map/presets.ts）0 问题（npx 沙箱受限，改用本地二进制）

### 步骤 5：L3 文档落行 + 收尾归档 ✅ 已完成（2026-08-28）

- L3 落行 6 处：`api-contracts.md` §7.44 /rank 口径段；041 `spec.md` 4 处（§3.2 端点表 rank 行 / §3.3 口径 + Service 层终处理 4 条 / §4.5.5 数据行 / P-5 风险表）；041 `data-model-extensions.md` §2.5（对比模式 + 字段表 5 行 + SQL 标题）
- 归档：本文件移入 `plans/done/`；roadmap §3 task 索引同步

## 3. 状态记录

| 日期 | 变更 |
| --- | --- |
| 2026-08-28 | task 创建 + 方案/排序键用户拍板 + roadmap §4 提案登记并会签 |
| 2026-08-28 | 后端实现：`_RANK_CITIES` 常量化 + `_fill_rank_city_full_set`（匹配规范化 + 缺市补 0）+ `_build_rank_response`（两组同序合计降序 + tiebreaker 市码升序 + 双组全 0 → regions: []）+ mock 阜新 (580, 3000)；测试重写 16 用例 → 13P/3F（3F = 基线登录 INVALID_PASSWORD_ENCRYPT，与本 task 无关） |
| 2026-08-28 | 前端改造：rank/presets.ts 删 RANK_CITY_ORDER + buildChartData 直用 local.regions；map/presets.ts CITY_NAMES 本地化；tsc + eslint 通过 |
| 2026-08-28 | L3 落行 6 处（api-contracts §7.44 + spec 端点表/§3.3/§4.5.5/P-5 + dme §2.5）；task 归档 done/ + roadmap §3 同步 |
