# Task · 2026-08-14-040-数据结构review问题记录

> 状态：✅ 完成（2026-08-17）
> 类型：L3 docs 维护（review only — 记录 review 发现，不直接改 spec）
> 创建：2026-08-14
> 完成：2026-08-17
> 前置：task-038（大屏 API 数据结构统一已完成）
> 关联依据：`docs/specs/038-bigdata-personnel-display/spec.md`、`docs/design/data-models.md`

## 完成小结（2026-08-17）

- **范围**：038 spec + 039 spec 数据结构 review（API 字段表 / SQL / 章节归属 / 字段注释 / `label` → `type` 改造等）。
- **成果**：
    - §3.1 已采纳 19 条（全部已落地修复到 spec.md / data-model-extensions.md / acceptance-tests.md，详见 `task-040 §4`）。
    - §3.4 已迁移 / 已关闭 7 条轨迹完整（详见 `task-040 §6`）。
    - §3.5 / §7 开放问题 11 条（→ spec.md §10，PM 决策后 L3 提案落地）。
    - §5 已全部清空（原 6 条 review 问题 §5.1 / §5.3 / §5.7 / §5.10 / §5.13 / §5.16 详细记录已迁至 `task-040 §6.4–§6.7`）。
    - §6 已迁移 / 已关闭 7 条详细轨迹（§6.1 / §6.2 / §6.3 / §6.4 O-12 / §6.5 O-13 / §6.6 O-20 / §6.7 O-21 承接原 O-2）。
    - §8 变更日志 24+ 条（含任务结构清理 / 决议记录 / 文件迁移等）。
    - 链接全部按 `docs-link-format.md` 合规化（`file:///` 绝对路径 10 处 + `../specs/` `../design/` 相对路径 7 处 + markdown 链接全部转纯文本）。
- **PM 决议记录（2026-08-17）**：
    - **O-12**：038 模块5 `threeDependents` 4 字段和 → **6 字段和（方案 A）**——3 类遗属 × 享/不享，不含老年子女。详见 `docs/specs/038-bigdata-personnel-display/spec.md#10-开放问题`。
    - **O-2 关闭**：原"§7.4 内容为 letter_screen_4"假设是错的（实际 §7.4 letter_screen_4 整段缺失），由新 O-21 取代。
- **遗留**（待 PM 决策 + L3 提案落地）：
    - O-13：038 `otherPersonnel` 改名 / 拆 3 项（与 O-12 同源）
    - O-20：039 §7.3 + §7.5 章节标题 + 字段注释错位
    - O-21：039 §7 缺 letter_screen_4 + 章节编号整体错位（承接原 O-2）
    - 其他开放问题（O-3 / O-4 / O-9 / O-10 / O-14 / O-15 / O-16 / O-17 / O-19）按 spec.md §10 决策
- **沉淀脚本**：`scripts/fix-doc-links.mjs`（按 docs-link-format.md 规则自动化合规链接转换，支持 `--check` / `--dry-run` / 默认 `write` 三种模式 + 代码块保护 + 幂等）

---

## 0. 任务信息

| 项        | 值                                                                                                                                                    |
| --------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 编号-slug | 2026-08-14-040-数据结构review问题记录                                                                                                                 |
| 任务类型  | L3 docs 维护（review only）                                                                                                                           |
| 影响范围  | `docs/specs/038-bigdata-personnel-display/spec.md`（逐步扩展到 039/040/041）                                                                          |
| 验收标准  | ① 每发现一处问题即记录到本文件 §3（问题台账） ② 每条问题注明：位置 / 现状 / 期望 / 阻塞 / 是否需要 PM 介入 ③ review 完成后由用户决定是否开新修复 task |
| 优先级    | 中 — 由用户逐步推进，不一次性铺开                                                                                                                     |

---

## 1. 背景

task-038 完成 038 + 039 spec §3.2/§3.3 数据结构统一后，用户启动对 038 spec 的逐模块 review。

- 本 task 是 **review 记录载体**，**不直接修改 spec**
- 每次 review 发现新问题，往 §3「问题台账」追加一条
- 用户 review 完所有模块后，再决定：
    - 哪些问题升级为修复 task（如 task-041、task-042...）
    - 哪些问题直接采纳为 spec 修订批次
    - 哪些问题转交 PM 决策

**工作分工**：

- review 出来的问题（含字段错配 / 旧结构 / 引用不一致）→ 记 `task-040 §3`
- 开放性问题（需 PM 决策的设计建议 / 待决策）→ 记 `spec.md §10`

---

## 2. 执行步骤

### 步骤 1：模块2（数字卡片列表）review ✅

- **动作**：核对 038 spec §3.2 / §3.3 模块2 的 2 个端点（`/total` + `/total/detail`）
- **结论**：数据结构统一、字段命名正确、`unit` 携带完整
- **等待用户**：否

### 步骤 2：模块5（优抚对象人数统计）review ✅ [部分]

- **动作**：核对 `preferential` 端点（§3.3 / 274-302 行）
- **前置副产品**：核对 `catPreObject` 10 项字段命名（与 data-models.md §8.2 对照）
- **等待用户**：是（决定模块5 整体口径是否继续 review）

### 步骤 3~6：模块1 / 3 / 4 / 6 review

- **3** 模块1 `/veteran` ✅（用户确认通过，无问题）
- **3** 模块3 `/map` ✅（§3.2 已采纳）
- **4** 模块4 `/region` ✅（§3.3 O-4 决议）
- **5** 模块5 `/preferential` ✅（§3.4-3.7 登记 4 个问题；3.7 迁移至 O-11）
- **6** 模块6 `/employment` ✅（§3.8-3.10 登记 + schema 改造）

- **等待用户**：是（每模块结束后确认下一步）

### 步骤 7~12：039 spec 6 端点 review

- **7** 039 模块1 `/trend` ✅（§3.11 schema 改造）
- **8** 039 模块2 `/appeal` ✅（§3.12 删 label）
- **9** 039 模块3 `/overview` ✅（§3.15 字段注释错误阻塞）
- **10** 039 模块4 `/map` ✅（§3.18 缺 letter_screen_4 阻塞）
- **11** 039 模块5 `/rank` ✅（§3.22 删 label）
- **12** 039 模块6 `/identity` ✅（§3.23 label → type 改造）

- **等待用户**：是（下一步：修复阻塞项 / 提 PM / 收尾）

---

## 3. 进度索引

> **分桶规则**：已采纳 / [038] 待办 / [039] 待办 / 已迁移-关闭 / 关联开放问题
> 详细记录见 §4（已采纳）、§5（待采纳）、§6（已迁移-关闭）、§7（开放问题）

### 3.1 已采纳（19 条）

| 编号  | 范围 | 标题                                               | 状态              |
| ----- | ---- | -------------------------------------------------- | ----------------- |
| §4.2  | 038  | 模块3/4 `data-model-extensions.md §2.4` 旧结构同步 | ✅                |
| §4.3  | 038  | O-4 决议：合并 `/map` + `/region` 为单端点         | ✅（待 L3 提案）  |
| §4.4  | 038  | `data-model-extensions.md §2.6` 旧结构同步         | ✅                |
| §4.5  | 038  | 模块6 schema 改造（label → type）                  | ✅                |
| §4.6  | 038  | spec.md 模块6 详述 SQL 段补全                      | ✅                |
| §4.7  | 039  | /trend `name` ↔ `label` 语义对调 + 删 label        | ✅                |
| §4.8  | 039  | /appeal 删 `label` 字段                            | ✅                |
| §4.9  | 039  | /rank 删 `label` 字段                              | ✅                |
| §4.10 | 039  | /identity `label → type` 改造                      | ✅                |
| §4.11 | 038  | `data-model-extensions.md §2.5` 旧结构同步         | ✅                |
| §4.12 | 039  | `data-model-extensions.md §2.2` 字段表部分采纳     | ✅（SQL 待 O-14） |
| §4.13 | 039  | `data-model-extensions.md §2.3` 派生命名同步       | ✅                |
| §4.14 | 039  | `data-model-extensions.md §2.4` 字段表同步         | ✅                |
| §4.15 | 039  | `data-model-extensions.md §2.5` 字段表 + SQL 同步  | ✅                |
| §4.16 | 039  | `data-model-extensions.md §2.6` 字段表同步         | ✅                |
| §4.17 | 038  | spec.md 模块5 `SELECT *` 引用改为 §2.5 SQL         | ✅                |
| §4.18 | 039  | spec.md §3.2 /appeal 索引表行同步                  | ✅                |
| §4.19 | 039  | spec.md §3.2 /identity 索引表行同步                | ✅                |
| §4.20 | 039  | /overview 8 项 key 统一 lowercase                  | ✅（方案 A）      |

### 3.2 [038] 待采纳 / 待决策（0 条）

> 038 模块5 全部 2 条 review 问题已迁移至 §6：§5.1 → §6.4（O-12 已决议关闭）/ §5.3 → §6.5（O-13 开放性问题）。

### 3.3 [039] 待采纳 / 待决策（0 条）

> 039 模块3/4 data-models.md §7 全部 4 条 review 问题已迁移至 §6：§5.7 + §5.16 → §6.6（O-20）/ §5.10 + §5.13 → §6.7（O-21，承接原 O-2）。

### 3.4 已迁移 / 已关闭（7 条）

| 编号 | 标题                                                                                  | 走向                                                                           |
| ---- | ------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| §6.1 | 模块5 `total` 拆分                                                                    | → spec.md §10 O-11（开放性问题）                                               |
| §6.2 | /rank `label` (MAX_TIME)                                                              | → §4.9 已采纳                                                                  |
| §6.3 | /appeal SQL `STATS_DATE` 策略                                                         | → spec.md §10 O-14（开放性问题）                                               |
| §6.4 | 模块5 `threeDependents` 4 字段和 → 6 字段和                                           | ✅ 已决议（O-12 关闭，方案 A）→ 待 L3 提案落地 spec.md §3.3 /preferential L303 |
| §6.5 | 模块5 `otherPersonnel` 字段名与内容语义错位                                           | → spec.md §10 O-13（开放性问题，待 PM 决策）                                   |
| §6.6 | data-models.md §7.3 + §7.5 章节标题 + 字段注释错位（合并 §5.7 + §5.16）               | → spec.md §10 O-20（开放性问题，待 PM 决策）                                   |
| §6.7 | data-models.md §7 缺 letter_screen_4 + 章节编号错位（合并 §5.10 + §5.13，承接原 O-2） | → spec.md §10 O-21（开放性问题，待 PM 决策）                                   |

### 3.5 关联开放问题（spec.md §10，需 PM 决策）

| 编号 | 范围 | 标题                                                                                  |
| ---- | ---- | ------------------------------------------------------------------------------------- |
| O-1  | 038  | `total` 取 `lxgb` / `yfdxhj` / `tyjryfdxhj`                                           |
| O-12 | 038  | 模块5 `threeDependents` 6 字段和（已决议 2026-08-17，方案 A）                         |
| O-13 | 038  | `otherPersonnel` 改名 / 拆 3 项（待 PM 决策）                                         |
| O-11 | 038  | 模块5 `total` 拆分                                                                    |
| O-14 | 039  | /appeal SQL 缺 `STATS_DATE` 取数策略                                                  |
| O-15 | 039  | /appeal `key` 是否含 `appealIdx` 后缀                                                 |
| O-16 | 039  | /overview API 8 项 vs UI 3 项展示                                                     |
| O-17 | 039  | /map `cantType` 参数用途                                                              |
| O-19 | 039  | /identity `GB_JXGB` "离休" vs PM"军休" 语义分歧                                       |
| O-20 | 039  | data-models.md §7.3 + §7.5 章节标题 + 字段注释错位（合并 §5.7 + §5.16）               |
| O-21 | 039  | data-models.md §7 缺 letter_screen_4 + 章节编号错位（合并 §5.10 + §5.13，承接原 O-2） |

---

## 4. 已采纳条目详细记录（9 条）

> 本节为 §3.1 中标 ✅ 条目的完整上下文，编号与上面索引一一对应。

### 4.2 038 模块3/4 `data-model-extensions.md §2.4` 旧结构同步

| 字段       | 值                                                                                                     |
| ---------- | ------------------------------------------------------------------------------------------------------ |
| 发现时间   | 2026-08-14                                                                                             |
| 发现位置   | `docs/specs/038-bigdata-personnel-display/data-model-extensions.md#L147` §2.4 / L147-150               |
| 现状       | API 字段为 `regions[].statsUnitName` + `regions[].veteran`（task-038 之前结构）；SQL 缺 `CANT_CODE` 列 |
| 期望       | API 字段为 `items[].key/name/value/unit` 4 行；SQL 含 `CANT_CODE` 列                                   |
| 阻塞       | 中（权威源 ≠ spec.md）                                                                                 |
| 需 PM 决策 | 否（机械同步）                                                                                         |
| 状态       | ✅ 已采纳                                                                                              |

**修复内容**（task-040 同步）：

1. 字段映射表改为 4 行：`items[].key (CANT_CODE)` + `items[].name (STATS_UNIT_NAME)` + `items[].value (VETERAN)` + `items[].unit (人)`
2. SQL 补 `CANT_CODE` 列：`SELECT CANT_CODE, STATS_UNIT_NAME, VETERAN FROM ...`
3. 追加「沈抚合并 2115 → 2104」特殊规则，与 spec.md §3.3 L270 一致

**审批依据**：`R-DOCS-038-UPGRADE` 目标文件清单含 `data-model-extensions.md`；本次为 task-038 落地后的机械同步。

### 4.3 038 O-4 决议：合并 `/map` 与 `/region` 为单端点

| 字段       | 值                                                                                                                                    |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| 原始问题   | spec.md §10 O-4 — 模块3和模块4数据源相同，是否复用同一端点                                                                            |
| 决议时间   | 2026-08-14                                                                                                                            |
| 决议       | 合并为 `/map` 单端点，删除 `/region`                                                                                                  |
| 依据       | 后端两个端点返回完全相同数据（同一张表、同一段 SQL、同一份 `items` 形状），仅前端单位换算不同；语义化 URL 收益不抵双端点实现/测试成本 |
| 状态       | ✅ 已采纳（用户决议） — **待 L3 提案审批**                                                                                            |
| 改动范围   | `docs/specs/038-bigdata-personnel-display/spec.md` §3.2 / §3.3 / §4.2 / §10                                                           |
| 需 PM 决策 | 否（已用户决定）                                                                                                                      |
| 阻塞       | low（端点合并需新提案）                                                                                                               |

**待办**：起草新提案 `R-DOCS-038-M34-MERGE`，等级 L3，删 `/region` 端点、§4.2 槽位 /region → /map、O-4 状态 → 关闭。

### 4.4 038 `data-model-extensions.md §2.6` 字段表同步（038 模块6）

| 字段       | 值                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                |
| 发现位置   | `docs/specs/038-bigdata-personnel-display/data-model-extensions.md#L195` §2.6 / L195-201  |
| 现状       | API 字段表为 `categories[]` + `series[].name/data[0..2]`（task-038 之前的高 charts 结构） |
| 期望       | `items[].key/name/value/unit/type` 5 行表示                                               |
| 阻塞       | 中（权威源 ≠ spec.md）                                                                    |
| 需 PM 决策 | 否（机械同步）                                                                            |
| 状态       | ✅ 已采纳                                                                                 |

**修复内容**（task-040 同步）：字段表改为 5 行 `items[].key` + `items[].name` + `items[].value` + `items[].unit` + `items[].type`，与 spec.md §3.3 /employment 改造后结构对齐。

**审批依据**：`R-DOCS-038-UPGRADE` 目标文件清单含 `data-model-extensions.md`。

### 4.5 038 模块6 schema 改造：name ↔ label 语义对调 + label → type

| 字段       | 值                                                                                                                                                                                   |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 发现时间   | 2026-08-14                                                                                                                                                                           |
| 修改前     | `items: [{ key, name: 人员类别, value, unit, label: 就业状态 }]`                                                                                                                     |
| 修改后     | `items: [{ key, name: 就业状态（X 轴）, value, unit, type: 人员类别（图例）}]`                                                                                                       |
| 改动依据   | 用户决议：name = 就业状态（X 轴分组），type = 人员类别（分组柱状图图例），删除 `label` 字段。type 字段符合 O-9 标准结构 `items: [{ key, name, value, unit, label?, ratio?, type? }]` |
| 阻塞       | 中（影响 §5.4 / §3.2 索引表）                                                                                                                                                        |
| 需 PM 决策 | 否（用户已决议）                                                                                                                                                                     |
| 状态       | ✅ 已采纳                                                                                                                                                                            |

**改动范围**：

1. spec.md §3.3 模块6 schema
2. name / label 取值表 → 重排为 name 取值表（就业状态 3 项）+ `type` 取值表（人员类别 4 项）
3. Service 层拍平规则同步：`name` = `cbzt` 中文名，`type` = `rylb` 中文名
4. `data-model-extensions.md §2.6` 字段表同步

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一 data 结构）；引入 `type` 是 O-9 已允许的可选字段。

### 4.6 038 spec.md 模块6 详述 SQL 段补全

| 字段                                   | 值                                                                                                |
| -------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 发现时间                               | 2026-08-14                                                                                        |
| 发现位置                               | `docs/specs/038-bigdata-personnel-display/spec.md#L340` §3.3 / L340-348                           |
| 现状                                   | spec.md 模块6 SQL 仅省级（无 WHERE 条件）；L351 文字说"地市级根据安置地匹配"但 SQL 没体现         |
| data-model-extensions.md §2.6 L216-224 | 地市级 SQL 完整（`WHERE "安置地" LIKE :cantCodePrefix \|\| '%'`）                                 |
| 期望                                   | spec.md 显式区分「省级 SQL」与「地市级 SQL」；地市级引用 `data-model-extensions.md §2.6` 完整 SQL |
| 阻塞                                   | low（实现按 §2.6 走即可）                                                                         |
| 状态                                   | ✅ 已采纳                                                                                         |

**修复内容**（task-040 同步）：

1. spec.md §3.3 /employment L339-348 拆分为「省级 SQL」（代码块）+「地市级 SQL」（引用 §2.6）
2. Service 层拍平规则修正：`name` 取 `cbzt` 中文名，`type` 取 `rylb` 中文名

### 4.7 039 模块1 `/trend` `name` ↔ `label` 语义错位

| 字段       | 值                                                                                                                                      |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                              |
| 发现位置   | `docs/specs/039-bigdata-petition-display/spec.md#L142` spec.md §3.3 / L142-146                                                          |
| 现状       | `items[].name` = 渠道（网上信访 / 来访 / 来信），`items[].label` = X 轴月份（YYYY-MM）；折线图分组键用 `name`，X 轴用 `label`           |
| 期望       | `items[].type` = 渠道（网上信访 / 来访 / 来信），`items[].name` = X 轴月份（YYYY-MM）；删除 `label`；前端分组键用 `type`，X 轴用 `name` |
| 阻塞       | low（仅 spec 文字改动）                                                                                                                 |
| 需 PM 决策 | 否（与 038 模块6 schema 改造一致：`R-DOCS-BIGSCREEN-API-UNIFORM` 已批）                                                                 |
| 状态       | ✅ 已采纳                                                                                                                               |

**修复内容**（task-040 同步）：

1. spec.md §3.3 /trend L142-146 字段重排 + 删 `label` + 加 `type`
2. spec.md §3.3 /trend L153 字段映射表表头 `name`/`label` → `type`/`name`
3. spec.md §3.3 /trend L159 警示文字「按 `name` 分组…按 `label` 升序…」→「按 `type` 分组…按 `name` 升序…」

**待办**（非本次修复范围）：

- `data-model-extensions.md §2.1` 仍是旧 `series[].name/data[]` 结构（与 spec.md 拍平后的 36 项 `items` 不一致），后续同步

### 4.8 039 模块2 `/appeal` 删 `label` 字段（无视图需求依据）

| 字段       | 值                                                                                                                                     |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                             |
| 发现位置   | `docs/specs/039-bigdata-petition-display/spec.md#L186` §3.3 / L186-192                                                                 |
| 现状       | `items[].label` = `REG_MAIN_APPEAL_ONE`（主要诉求一级分类），但 PM §4.2 + I-8 未要求 API 暴露该字段；环形饼图渲染仅需 `name` + `value` |
| 期望       | 删 `items[].label`；`REG_MAIN_APPEAL_ONE` 仅参与 `key` 组合                                                                            |
| 阻塞       | low（仅 spec 文字改动）                                                                                                                |
| 需 PM 决策 | 否（用户已确认）                                                                                                                       |
| 状态       | ✅ 已采纳                                                                                                                              |

**修复内容**（task-040 同步）：

1. spec.md §3.3 /appeal L186-192 删 `label` 字段
2. spec.md §3.3 /appeal L195-201 字段表删 `label` 行
3. spec.md §3.3 /appeal L212 Service 层映射说明同步删 `label`
4. spec.md §3.3 /appeal L214 图例说明删除「按 `REG_MAIN_APPEAL_ONE` 分组聚合」（环形饼图不需二级分类）

**依据**：PM §4.2 SQL 例子里有 `REG_MAIN_APPEAL_ONE` 列，但**仅是 SELECT 取数列**，未要求 API 响应暴露；图例按 `ITEM_NAME` 动态读取（I-8 确认），`REG_MAIN_APPEAL_ONE` 无 UI 角色。

### 4.9 039 模块5 `/rank` 删 `label` 字段（无视图需求依据）

| 字段       | 值                                                                                                                                                                                           |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                                                                   |
| 发现位置   | `docs/specs/039-bigdata-petition-display/spec.md#L304` spec.md §3.3 / L304-331                                                                                                               |
| 现状       | `items[].label` = `MAX_TIME`（最大时间）；PM §4.5 字段映射表里有 `MAX_TIME` 列但**未要求 API 暴露**；§4.5.5 视图需求只说柱状图 + 14 地市 + 数据标签（数值）+ 单位：万，`MAX_TIME` 无 UI 角色 |
| 期望       | 删 `items[].label`；`MAX_TIME` 不再出现在 API 响应（SQL 可后续清理，见 §5.12）                                                                                                               |
| 阻塞       | low（仅 spec 文字改动）                                                                                                                                                                      |
| 需 PM 决策 | 否（用户已确认；同 §4.8 模块2 label 删除先例）                                                                                                                                               |
| 状态       | ✅ 已采纳                                                                                                                                                                                    |

**修复内容**（task-040 同步）：

1. spec.md §3.3 /rank L310 删 `label` 字段
2. spec.md §3.3 /rank L315-319 字段表删 `label` 列（剩 3 列：key / name / unit）
3. spec.md §3.3 /rank L331 Service 层映射说明同步删 `label=MAX_TIME`

**依据**：PM §4.5 视图只列柱状图 + 数据标签（柱顶数值）+ 单位换算，"最大时间" 在 §5.2 时间显示中另设全局组件，与每地区时间无关。

**待办**：

- spec.md §3.3 /rank SQL 段 L324 仍 `SELECT MAX_TIME` — label 删后该列变成死列；后续 §5.12 sync 时一并清理
- §3.2 L100 索引表仍写 `items: [{ key, name, value, unit, label }]` — 与 §3.3 不一致（用户要求"其他的不动"，留作 §5.12 后续处理）

### 4.10 039 模块6 `/identity` `label` → `type` 改造（与模块1 /trend 同模式）

| 字段       | 值                                                                                                                                      |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                              |
| 发现位置   | `docs/specs/039-bigdata-petition-display/spec.md#L348` §3.3 / L348 / L369 / L384                                                        |
| 现状       | `items[].label` = 渠道（网上信访 / 来访 / 来信），但渠道是分类（type），不是标签；与 038 模块6 /employment 模式一致（§4.5 schema 改造） |
| 期望       | `items[].type` = 渠道（网上信访 / 来访 / 来信）                                                                                         |
| 阻塞       | low（仅 spec 文字改动）                                                                                                                 |
| 需 PM 决策 | 否（用户已确认；同 038 模块6 + 039 模块1 已批）                                                                                         |
| 状态       | ✅ 已采纳                                                                                                                               |

**修复内容**（task-040 同步）：

1. spec.md §3.3 /identity L348 `label: string; // 渠道` → `type: string; // 渠道`
2. spec.md §3.3 /identity L369 `label` → `type`（图例取值说明）
3. spec.md §3.3 /identity L384 Service 层映射 `label` → `type`

**依据**：分组柱状图渲染时 `type` 作为分组维度（series），与 ECharts `series: [{ name, data }]` 的 `name` 字段语义对齐；`label` 仅适合 tooltip / 数据标签等附加信息，不适合做分组维度。

### 4.11 038 `data-model-extensions.md §2.5` 字段表同步（038 模块5）

| 字段       | 值                                                                                                                                                                                      |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                                                              |
| 发现位置   | `docs/specs/038-bigdata-personnel-display/data-model-extensions.md#L170` §2.5 / L170-178                                                                                                |
| 现状       | API 字段表为旧 `API 字段 / DB 列 / 算法` 三列结构（task-038 之前）；与 §2.4 / §2.6 同问题                                                                                               |
| 期望       | 上方 4 行 `items[].key / name / value / unit` 通用结构（与 §2.4 对齐）+ 下方「各 `key` 取值规则」表逐项列出 7 个 key 对应的 name / DB 列 / 表达式（与 spec.md §3.3 /preferential 对齐） |
| 阻塞       | 中（权威源 ≠ spec.md）                                                                                                                                                                  |
| 需 PM 决策 | 否（机械同步）                                                                                                                                                                          |
| 状态       | ✅ 已采纳                                                                                                                                                                               |

**修复内容**（task-040 同步）：

1. §2.5 上方字段表改为 4 行 `items[].key / name / value / unit`，与 §2.4 / §2.6 模式一致
2. §2.5 下方新增「各 `key` 取值规则」表，逐项列出 7 个 key 对应的 `name`（PM §F.5）+ DB 列 / 表达式（与 spec.md §3.3 /preferential L296-304 字段映射表完全一致）
3. SQL 段（L191-196）保持不动：已显式列 12 字段（task-038 后结构），与新字段表一致
4. 引用 `spec §10 O-1`（`total` 取 `lxgb`）保持；§5.1 / §5.3 / O-11 等语义问题不在本修复范围

**依据**：

- spec.md §3.3 /preferential L296-304 已采纳新结构（task-038 后），本次为 data-model-extensions.md 对齐 spec.md 的机械同步
- `key` 命名 `total / disabledVeteran / ruralVeteran / sickReturnVeteran / ruralAged60 / threeDependents / otherPersonnel` 取自 PM §F.5（`pm-requirements-input.md`），与 spec.md 一致
- `name` 中文名取自 spec.md §3.3 /preferential L296-304（与 PM §F.5 指标列一致）
- DB 列名取自 data-models.md §8.2（task-038 已采纳的字段定义权威源）

**审批依据**：`R-DOCS-038-UPGRADE` 目标文件清单含 `data-model-extensions.md`；本次为 task-038 落地后的机械同步（与 §4.2 / §4.4 同模式）。

**未触动**：

- §5.1 `threeDependents` 4 字段和 vs 6 字段和（待 PM 决策）
- §5.3 `otherPersonnel` 命名/拆分（待 PM 决策）
- O-11 `total` 顶层字段拆分（依赖 O-1 决议）

### 4.12 039 `data-model-extensions.md §2.2` 字段表部分采纳（SQL 待 O-14）

| 字段       | 值                                                                                                                                                                                               |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 发现时间   | 2026-08-14                                                                                                                                                                                       |
| 发现位置   | `docs/specs/039-bigdata-petition-display/data-model-extensions.md#L61` data-model-extensions.md §2.2 / L61-83                                                                                    |
| 现状       | API 字段表为旧 `items[].itemName / appealCategory / num` 三列结构（task-038 之前）；SQL 缺 `STATS_DATE` 维度取数策略；与 spec.md §3.3 /appeal 拍平后的 4 行 `items[].key/name/value/unit` 不一致 |
| 期望       | 上方 4 行 `items[].key / name / value / unit` 通用结构；SQL 段补 `STATS_DATE` 取数策略（参 O-14）                                                                                                |
| 阻塞       | low（字段表部分机械同步；SQL 段依赖 O-14 决议）                                                                                                                                                  |
| 需 PM 决策 | 部分（SQL `STATS_DATE` 取数策略需 O-14 决议）                                                                                                                                                    |
| 状态       | ✅ 部分采纳（字段表已同步；SQL 段待 O-14 决议后补）                                                                                                                                              |

**修复内容**（task-040 同步）：

1. §2.2 上方字段表改为 4 行 `items[].key / name / value / unit`，与 spec.md §3.3 /appeal L195-200 + §4.8 删 `label` 决议对齐
2. `items[].key` = `ITEM_NAME` + `REG_MAIN_APPEAL_ONE` 后端组合（形如 `appeal_<itemNameIdx>_<appealIdx>`），与 spec.md §3.3 /appeal 一致
3. `items[].unit` = `"件"`（与 §3.3 /appeal 一致）
4. §2.2 文末新增引用：明确 `REG_MAIN_APPEAL_ONE` 仅参与 `key` 组合、不暴露给前端（与 §4.8 删 `label` 决议一致）
5. SQL 段保持不动（task-038 之前结构），新增 ⚠️ 警示说明 `STATS_DATE` 取数策略依赖 O-14；SQL 段待 O-14 决议后再补

**未触动（迁移至 §6.3）**：

- SQL `STATS_DATE` 取数策略 → spec.md §10 O-14（开放性问题，待 PM 决策）
- SQL 候选策略：① `MAX(STATS_DATE)` 取最新 ② 按日期范围 `SUM` ③ 默认只 1 行无需聚合

**依据**：

- spec.md §3.3 /appeal L195-200 已采纳新结构（task-038 后），本次为 data-model-extensions.md 对齐 spec.md 的机械同步（与 §4.2 / §4.4 / §4.11 同模式）
- spec.md §10 O-14 已识别 SQL `STATS_DATE` 策略开放问题，本次不擅自决定，保留待 PM

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一 data 结构）；字段表同步不涉及业务语义变更。

### 4.13 039 `data-model-extensions.md §2.3` 派生命名同步

| 字段       | 值                                                                                                                                                   |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                           |
| 发现位置   | `docs/specs/039-bigdata-petition-display/data-model-extensions.md#L111` §2.3 / L111-115                                                              |
| 现状       | 派生卡片表使用英文字段名 `yesterdayTotal / yesterdayJtfpc / yesterdayJtfrc`；表头为「派生卡片 / 公式」                                               |
| 期望       | 派生卡片表使用中文卡片名「昨日信访量 / 昨日集体访批次 / 昨日集体访人次」；表头改为「卡片名称 / 取值字段」（与 spec.md §3.3 /overview L245-249 对齐） |
| 阻塞       | low                                                                                                                                                  |
| 需 PM 决策 | 否（机械同步）                                                                                                                                       |
| 状态       | ✅ 已采纳                                                                                                                                            |

**修复内容**（task-040 同步）：

1. §2.3 派生表表头 `派生卡片 / 公式` → `卡片名称 / 取值字段`，与 spec.md §3.3 /overview L245-249 一致
2. §2.3 派生卡片行：
    - `yesterdayTotal` → `昨日信访量`
    - `yesterdayJtfpc` → `昨日集体访批次`
    - `yesterdayJtfrc` → `昨日集体访人次`
3. 取值公式列改为「取值字段」用语，与 spec.md 一致
4. 章节小标题 `前端派生字段` → `卡片派生（前端展示，仅展示用，不入 API data）`，与 spec.md §3.3 /overview L243 小标题对齐
5. SQL 段不动（无变更需求）

**依据**：

- spec.md §3.3 /overview L243-249「卡片派生（前端展示）」表已用中文卡片名 + 取值字段格式，本次为 data-model-extensions.md 对齐 spec.md 的机械同步（与 §4.2 / §4.4 / §4.11 同模式）
- 派生卡片**不入 API `data`**（与 spec.md §3.3 /overview L243 表述一致；前端展示层职责）

**未触动**：

- §5.8 /overview `key` 大小写风格混用（独立 issue；与本条目无直接关联）

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一卡片命名）；不涉及业务语义变更。

### 4.14 039 `data-model-extensions.md §2.4` 字段表同步（039 模块4）

| 字段       | 值                                                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                |
| 发现位置   | `docs/specs/039-bigdata-petition-display/data-model-extensions.md#L123` data-model-extensions.md §2.4 / L123-127                          |
| 现状       | API 字段表用旧 `regions[].cantName / cantCode / num` 三字段结构；与 spec.md §3.3 /map 拍平后的 `items[].key / name / value / unit` 不一致 |
| 期望       | 字段表改为 4 行（`items[].key / name / value / unit`）+ SQL 同步 CANT_CODE/CANT_NAME 顺序 + 新增沈抚合并特殊规则引用                      |
| 阻塞       | low（机械同步）                                                                                                                           |
| 需 PM 决策 | 否                                                                                                                                        |
| 状态       | ✅ 已采纳                                                                                                                                 |

**修复内容**（task-040 同步）：

1. §2.4 字段表改为 4 行 `items[].key / name / value / unit`：
    - `items[].key` = `CANT_CODE`
    - `items[].name` = `CANT_NAME`
    - `items[].value` = `NUM`
    - `items[].unit` = `"件"`（常量）
2. SQL 字段顺序调整为 `SELECT CANT_CODE, CANT_NAME, NUM`（与 spec.md §3.3 /map L288 一致）
3. §2.4 文末新增「特殊规则」行：引用 §4.1 沈抚合并规则（PM I-9 已确认）
4. SQL 段 WHERE 条件不动（REG_AUTHORITY / ORGAN_ID 与 §3.3 /map 一致）

**依据**：

- spec.md §3.3 /map L268-296 已采纳新结构（task-038 后），本次为 data-model-extensions.md 对齐 spec.md 的机械同步（与 §4.2 / §4.4 / §4.11 同模式）
- 沈抚合并规则已在本文件 §4.1 详细定义，§2.4 仅引用避免重复

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一 data 结构）；不涉及业务语义变更。

### 4.17 038 spec.md 模块5 详述口径 `SELECT *` 引用改为 `data-model-extensions.md §2.5` SQL

| 字段       | 值                                                                                                                                                                                                 |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                                                                         |
| 发现位置   | `docs/specs/038-bigdata-personnel-display/spec.md#L306` spec.md L306                                                                                                                               |
| 现状       | spec.md L306 口径行写 `SELECT * FROM abi_zb_tyjrjqyfdx WHERE cant_code = #{cantCode}`；与字段映射表（7 项 `key`）范围不一致（实际只需要 12 个字段），且 `SELECT *` 不符合 §4.6「口径只写引用」原则 |
| 期望       | 改为引用 `data-model-extensions.md §2.5` 12 字段 SQL（§4.11 已采纳），口径行形如 `SELECT <12 字段> FROM abi_zb_tyjrjqyfdx WHERE cant_code = #{cantCode}`，SQL 段不在 spec.md 重复                  |
| 阻塞       | low（机械同步）                                                                                                                                                                                    |
| 需 PM 决策 | 否                                                                                                                                                                                                 |
| 状态       | ✅ 已采纳                                                                                                                                                                                          |

**修复内容**（task-040 同步）：

1. spec.md L306 口径行：`SELECT *` → `SELECT <12 字段>`，并加引用 ``./data-model-extensions.md#25-模块5优抚对象分类preferential` data-model-extensions.md §2.5`+ 标注`(task-040 §4.11 已同步)`
2. 与 spec.md L241 /preferential「SQL 引用」段同模式（口径段只写引用，不复制 SQL）
3. Service 层映射描述不变（7 项 `items`，其中 `threeDependents` / `otherPersonnel` 多字段求和派生）

**依据**：

- `data-model-extensions.md §2.5` SQL 段已显式列出 12 字段（task-040 §4.11 ✅）；spec.md L306 写 `SELECT *` 与之矛盾
- spec.md §5.2.5 已声明「口径见 `./data-model-extensions.md` data-model-extensions.md」，L306 应遵循该原则（不写 `SELECT *`，引用 §2.5 即可）
- 与 spec.md L241（模块2 下钻子集 /total/detail）口径「SQL 引用」段模式一致

**未触动**：

- spec.md L298-304 字段映射表（7 项 `key` → 取值规则）不变
- spec.md L308 O-1 警示（`lxgb` 取值异常）不变

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一口径引用模式）；不涉及业务语义变更。

### 4.18 039 spec.md §3.2 /appeal 索引表行同步

| 字段       | 值                                                                                                                                                                            |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                                                    |
| 发现位置   | `docs/specs/039-bigdata-petition-display/spec.md#L97` §3.2 / L97                                                                                                              |
| 现状       | L97 `/appeal` 行写 `items: [{ key, name, value, unit, label }]` × N 项（按 `ITEM_NAME × REG_MAIN_APPEAL_ONE` 聚合）；与 §4.8 删 label 决议 + §4.12 §2.2 字段表 4 行结构不一致 |
| 期望       | L97 改为 `items: [{ key, name, value, unit }]` × N 项（N = 实际事项数）；删除「按 `ITEM_NAME × REG_MAIN_APPEAL_ONE` 聚合」表述                                                |
| 阻塞       | low（内部 spec 不一致）                                                                                                                                                       |
| 需 PM 决策 | 否                                                                                                                                                                            |
| 状态       | ✅ 已采纳                                                                                                                                                                     |

**修复内容**（task-040 同步）：

1. spec.md §3.2 L97 字段列：`items: [{ key, name, value, unit, label }]` × N 项（按 `ITEM_NAME × REG_MAIN_APPEAL_ONE` 聚合）→ `items: [{ key, name, value, unit }]` × N 项（N = 实际事项数）
2. 删除「按 `ITEM_NAME × REG_MAIN_APPEAL_ONE` 聚合」表述（与 §4.8 / §4.12 决议一致：`REG_MAIN_APPEAL_ONE` 仅参与 `key` 后端组合，不暴露给前端，索引表用户视角只看 ITEM_NAME 一维）
3. N 项说明改为「N = 实际事项数」（消除聚合歧义；与 §3.3 /appeal L189-200 一致）

**依据**：

- spec.md §3.3 /appeal 已删 `label` 字段（task-040 §4.8 ✅）；§3.3 L193-200 拍平为 `items[].key/name/value/unit`，索引表 L97 与详述不一致
- §4.12 / §4.16 已明确：`REG_MAIN_APPEAL_ONE` 不暴露给前端，仅参与 `key` 后端组合；前端「事项」维度看就是 N 个 ITEM_NAME
- 与 §3.2 /map L99（`× 14 项（按 cantCode 唯一）`）同模式：「按 X 唯一」替代「按 X × Y 聚合」

**未触动**：

- §5.14 spec.md §3.2 索引表（L101）/identity 不一致（独立 issue；与本条目无直接关联）
- §3.2 其他行（L96 /trend、L100 /rank、L101 /identity）仍含 `label`（独立 review 问题；不在本条目范围）

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一 data 结构索引）；不涉及业务语义变更。

### 4.20 039 模块3 `/overview` 8 项 `key` 统一 lowercase（方案 A）

| 字段       | 值                                                                                                                        |
| ---------- | ------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                |
| 发现位置   | `docs/specs/039-bigdata-petition-display/spec.md#L232` spec.md §3.3 / L232-249                                            |
| 现状       | 8 项 `key` 中 6 项 all-lowercase（`jtwx/zwx/jtlf/zlf/zlx/jtlx`），2 项 camelCase（`jtJtfpc/jtJtfrc`），同一模块内风格混用 |
| 期望       | 统一为一种风格：方案 A 全 lowercase；方案 B 全 camelCase                                                                  |
| 阻塞       | low（代码风格问题）                                                                                                       |
| 需 PM 决策 | 否（机械统一）                                                                                                            |
| 状态       | ✅ 已采纳（**方案 A**）                                                                                                   |

**修复内容**（task-040 同步）：

1. spec.md §3.3 /overview L238-239 主表 `key` 列：`jtJtfpc` → `jtjtfpc`，`jtJtfrc` → `jtjtfrc`（DB 列 `JTJTFPC`/`JTJTFRC` 不变）
2. spec.md §3.3 /overview L248-249 派生卡片表「取值字段」列同步：`jtJtfpc` → `jtjtfpc`，`jtJtfrc` → `jtjtfrc`
3. data-model-extensions.md §2.3 L98-99 字段表 `key` 列同步：`jtJtfpc` → `jtjtfpc`，`jtJtfrc` → `jtjtfrc`（DB 列不变）
4. data-model-extensions.md §2.3 L118-119 派生卡片表「取值字段」列同步：`jtJtfpc` → `jtjtfpc`，`jtJtfrc` → `jtjtfrc`
5. acceptance-tests.md L51-52 BDD 用例期望值同步：`jtJtfpc` → `jtjtfpc`，`jtJtfrc` → `jtjtfrc`

**方案选择说明（方案 A）**：

- 选 A（lowercase）而非 B（camelCase）：8 项 key 中 6 项已是 lowercase（`jtwx/zwx/jtlf/zlf/zlx/jtlx`），方案 A 仅改 2 项；方案 B 要改 6 项 + 影响范围更大
- DB 列全大写（`JTWX/JTJTFPC` 等），lowercase 转换与 lowercase(db_column) 习惯更一致
- 接受风险：前端 TS 类型如有 camelCase 假设需同步改（与 acceptance-tests.md 已同步，§4.20 落地完整）

**依据**：

- 原 §5.8 条目已标 "需 PM 决策: 否（机械统一）"，用户确认不需 PM 决议
- 方案选择结果在 §3.1 「状态」列标注 `✅（方案 A）` 透明记录

**未触动**：

- DB 列名（`JTWX/JTJTFPC/...`）不变（仅 API key 风格统一）
- 模块4 / 模块5 的 camelCase `cantCode` 等不动（独立模块，独立 review）
- §5.7 / §5.10 / §5.13 / §5.16 等 data-models.md 章节结构问题（与本条目无直接关联）

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（API key 风格统一）；不涉及业务语义变更；仅机械字符替换。

### 4.19 039 spec.md §3.2 /identity 索引表行同步

| 字段       | 值                                                                                                                                        |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                |
| 发现位置   | `docs/specs/039-bigdata-petition-display/spec.md#L101` spec.md §3.2 / L101                                                                |
| 现状       | L101 `/identity` 行写 `items: [{ key, name, value, unit, label }]` × 33 项；与 §4.10 label → type 改造 + §4.16 §2.6 字段表 5 行结构不一致 |
| 期望       | L101 改为 `items: [{ key, name, value, unit, type }]` × 33 项                                                                             |
| 阻塞       | low（内部 spec 不一致）                                                                                                                   |
| 需 PM 决策 | 否                                                                                                                                        |
| 状态       | ✅ 已采纳                                                                                                                                 |

**修复内容**（task-040 同步）：

1. spec.md §3.2 L101 字段列：`items: [{ key, name, value, unit, label }]` → `items: [{ key, name, value, unit, type }]`（字段重命名：`label` → `type`，与 §4.10 / §4.16 一致）
2. 行数 / 描述不变（仍为 33 项 = 11 类身份 × 3 渠道）

**依据**：

- spec.md §3.3 /identity L348 已采纳 `type: string` 字段（task-040 §4.10 ✅ label → type 改造）
- §4.16 `data-model-extensions.md §2.6` 字段表已同步为 5 行 `items[].key/name/value/unit/type` + 「`type` 取值表」(3 行：网上信访/来访/来信)
- 索引表 L101 与 §3.3 /identity L348 详述 + §4.16 数据模型一致

**未触动**：

- §3.2 其他行（L96 /trend、L100 /rank 仍含 `label`）—— 独立 review 问题；不在本条目范围
- §5.16 data-models.md §7.3 章节标题 + 字段注释错位（与 §5.7 / §5.13 同源，data-models.md 章节结构问题，与本条目无直接关联）

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一 data 结构索引）；不涉及业务语义变更。

### 4.16 039 `data-model-extensions.md §2.6` 字段表同步（039 模块6）

| 字段       | 值                                                                                                                                                                                                    |
| ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                                                                            |
| 发现位置   | `docs/specs/039-bigdata-petition-display/data-model-extensions.md#L168` §2.6 / L168-213                                                                                                               |
| 现状       | API 字段表为旧双层结构 `categories[0..10]`（X 轴分类）+ `series[0..2].name/data[0..10]`（3 渠道 × 11 身份）；与 spec.md §3.3 /identity 拍平后的 `items[].key/name/value/unit/type` 不一致             |
| 期望       | 字段表改为通用 5 行（`items[].key/name/value/unit/type`）；下方新增「`name` 取值表」（11 项身份类型 → DB 列映射）+「`type` 取值表」（3 渠道 → `ACCEPT_SOURCE` 映射）；SQL 段不动（11 个身份字段不变） |
| 阻塞       | low（机械同步）                                                                                                                                                                                       |
| 需 PM 决策 | 否                                                                                                                                                                                                    |
| 状态       | ✅ 已采纳                                                                                                                                                                                             |

**修复内容**（task-040 同步）：

1. §2.6 字段表改为通用 5 行 `items[].key / name / value / unit / type`：
    - `items[].key` = `ACCEPT_SOURCE` + 身份字段 后端组合（形如 `identity_<identityIdx>_<src>`）
    - `items[].name` = 身份类型（X 轴分组，11 项）
    - `items[].value` = 各身份字段值（Service 层拍平 33 项 = 11 × 3）
    - `items[].unit` = `"件"`（常量）
    - `items[].type` = 渠道（分组柱状图图例，`ACCEPT_SOURCE` 1/2/3）
2. 下方新增「`name` 取值表」（11 行）：中文身份名 → DB 列（与 spec.md §3.3 /identity L353-367 一致）
3. 下方新增「`type` 取值表」（3 行）：`网上信访/来访/来信` → `ACCEPT_SOURCE=1/2/3`
4. SQL 段不动（保持按 `ACCEPT_SOURCE` 分三组取 11 个身份字段）
5. 文末新增 ⚠️ 警示：① I-7 / I-10 决议（11 类不扩展、ACCEPT_SOURCE=7 不展示）② GB_ZYJZGB / GB_JXGB 命名分歧已迁移 spec.md §10 O-19

**依据**：

- spec.md §3.3 /identity L339-384 已采纳新结构（task-038 后 + §4.10 label → type 改造），本次为 data-model-extensions.md 对齐 spec.md 的机械同步（与 §4.2 / §4.4 / §4.11 / §4.14 / §4.15 同模式）
- O-19 命名分歧：原 §2.6 文末仅引用 data-models.md §7.3，未明确指向 O-19；本次同步显式指向 spec.md §10 O-19，避免误读为已闭项

**未触动**：

- §5.14 spec.md §3.2 索引表（L101）与 §3.3 /identity 不一致（独立 issue；与本条目无直接关联）

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一 data 结构）；不涉及业务语义变更。

### 4.15 039 `data-model-extensions.md §2.5` 字段表 + SQL 同步（039 模块5）

| 字段       | 值                                                                                                                                          |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                  |
| 发现位置   | `docs/specs/039-bigdata-petition-display/data-model-extensions.md#L144` data-model-extensions.md §2.5 / L144-149                            |
| 现状       | API 字段表用旧 `regions[].cantName / cantCode / num / maxTime` 四字段结构；SQL 段 `SELECT MAX_TIME`（label 已删后变死列，task-040 §4.9 ✅） |
| 期望       | 字段表改为 4 行（`items[].key / name / value / unit`，删除 `maxTime`）；SQL 段同步删 `MAX_TIME` 列                                          |
| 阻塞       | low（机械同步；O-18 已关闭，独立处理）                                                                                                      |
| 需 PM 决策 | 否                                                                                                                                          |
| 状态       | ✅ 已采纳                                                                                                                                   |

**修复内容**（task-040 同步）：

1. §2.5 字段表改为 4 行 `items[].key / name / value / unit`（同 §4.14 模式）：
    - `items[].key` = `CANT_CODE`
    - `items[].name` = `CANT_NAME`
    - `items[].value` = `NUM`
    - `items[].unit` = `"件"`（常量）
    - 删除 `regions[].maxTime` 行（与 §4.9 删 label 决议一致）
2. SQL 段同步删 `MAX_TIME` 列：`SELECT CANT_CODE, CANT_NAME, NUM`（与 §4.14 /map 同结构）
3. SQL 段尾新增 ⚠️ 警示说明：MAX_TIME 列已删理由 + 引用 §4.4「天粒度取数」作为替代（如仍需 `MAX_TIME` 取数走全局组件）

**依据**：

- spec.md §3.3 /rank L298-333 已采纳新结构（task-038 后 + §4.9 删 label），本次为 data-model-extensions.md 对齐 spec.md 的机械同步
- SQL `MAX_TIME` 列在 label 字段已删后变死列（task-040 §4.9 已确认无 UI 角色）
- PM I-9 沈抚合并规则同样适用本端点（与 §4.14 /map 同模式；§2.5 文末未显式加「特殊规则」行，因 spec.md §3.3 /rank L333 已说明「同模块 4」）

**审批依据**：`R-DOCS-BIGSCREEN-API-UNIFORM` 范围（统一 data 结构）；不涉及业务语义变更。

---

## 5. 待采纳 / 待决策条目详细记录（0 条 / 原 6 条已全部迁移）

> 本节已全部清空。原 6 条 review 问题（038: §5.1 / §5.3 + 039: §5.7 / §5.10 / §5.13 / §5.16）已分别迁移至 §6.4 / §6.5 / §6.6 / §6.7，决议沉淀至 `docs/specs/038-bigdata-personnel-display/spec.md#10-开放问题` §10 O-12 / O-13 和 `docs/specs/039-bigdata-petition-display/spec.md#10-开放问题` §10 O-20 / O-21（原 O-2 同时关闭）。

---

## 6. 已迁移 / 已关闭条目（3 条）

> 本节为 §3.4 中走"迁移"或"关闭"路径的条目，记录原 review 发现 + 走向。

### 6.1 038 模块5 `total` 拆分（迁移 → spec.md §10 O-11）

| 字段       | 值                                                                       |
| ---------- | ------------------------------------------------------------------------ |
| 发现时间   | 2026-08-14                                                               |
| 关联位置   | `docs/specs/038-bigdata-personnel-display/spec.md#L286` spec.md#L286-293 |
| 现状       | `items` 7 项含 `total`（与模块1 `/veteran` 模式不一致）                  |
| 期望       | 拆出顶层 `total: { name, value, unit }` + items 减为 6 项，与模块1 一致  |
| 待办       | 与 O-1（`total` 取 `lxgb` 决策）绑定处理；先决 O-1 再决本拆分            |
| 需 PM 决策 | 是（先决 O-1，本项随之决议）                                             |
| 状态       | ✅ 已迁移至 spec.md §10 O-11（开放性问题，非 review 问题）               |

> **备注**：按工作分工，开放性问题归 `spec.md §10`，review 问题归 `task-040 §5`。

### 6.2 039 模块5 `/rank` `label` (MAX_TIME) 视图需求（已采纳 → §4.9）

| 字段     | 值                                    |
| -------- | ------------------------------------- |
| 发现时间 | 2026-08-14                            |
| 原始位置 | spec.md §10 O-18                      |
| 现状     | §3.3 /rank label 字段视图侧无 UI 角色 |
| 决议     | 用户采纳删除 label，§4.9 已采纳       |
| 状态     | ✅ 已采纳（详见 §4.9）                |

### 6.3 039 模块2 `/appeal` SQL `STATS_DATE` 取数策略（迁移 → spec.md §10 O-14）

| 字段       | 值                                                                                                                 |
| ---------- | ------------------------------------------------------------------------------------------------------------------ |
| 发现时间   | 2026-08-14                                                                                                         |
| 关联位置   | `docs/specs/039-bigdata-petition-display/data-model-extensions.md#L82` data-model-extensions.md §2.2 / L82-84      |
| 原问题     | §5.5 字段表 + SQL 段旧结构未同步；本次字段表已采纳（§4.12），SQL `STATS_DATE` 取数策略保留待 O-14 决议             |
| 现状       | data-model-extensions.md §2.2 SQL 段未含 `STATS_DATE` 维度策略；§2.2 已加 ⚠️ 警示说明依赖 O-14                     |
| 待办       | O-14 决议后回填 `data-model-extensions.md §2.2` SQL 段（候选：① `MAX(STATS_DATE)` ② 按日期范围 `SUM` ③ 默认 1 行） |
| 需 PM 决策 | 是（O-14）                                                                                                         |
| 状态       | ✅ SQL 部分已迁移至 spec.md §10 O-14（开放性问题）；字段表部分已采纳（§4.12）                                      |

> **备注**：按工作分工，开放性问题归 `spec.md §10`，review 问题归 `task-040 §5`；本次拆分采纳：字段表 → §4.12；SQL → O-14。

### 6.4 038 模块5 `threeDependents` 4 字段和 → 6 字段和（已决议 → spec.md §10 O-12）

| 字段       | 值                                                                                                                                                             |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14（PM 决议 2026-08-17）                                                                                                                               |
| 关联位置   | `docs/specs/038-bigdata-personnel-display/spec.md#L297` spec.md §3.3 /preferential L297                                                                        |
| 原问题     | §5.1 字段和定义错误：4 字段和（含老年子女 `yfxsgjdqbzlslnzn` + 漏 3 不享），与 PM 同文件 §F.5 L302-308 / 月报 L665-674「其他优抚对象」字段清单 7 项自相矛盾    |
| 决议       | 2026-08-17 **方案 A（合并 6 字段和）**：3 类遗属 × 享/不享，不含老年子女。详见 `docs/specs/038-bigdata-personnel-display/spec.md#10-开放问题` spec.md §10 O-12 |
| 待办       | 提 L3 提案 `R-DOCS-038-M5-THREE-DEPENDENTS-FIX`，修改 spec.md §3.3 /preferential L297 公式 + data-model-extensions.md §2.5 SQL 段 + acceptance-tests.md        |
| 需 PM 决策 | 否（已决议）                                                                                                                                                   |
| 状态       | ✅ 已决议（O-12 关闭）→ 待 L3 提案落地                                                                                                                         |

> **备注**：按工作分工，开放性问题归 `spec.md §10`，review 问题归 `task-040 §5`。§5.1 详细分析（6 字段表 / 3 方案表 / 决议依据 4 条 / 后续动作 4 项）已沉淀至 `docs/specs/038-bigdata-personnel-display/spec.md#10-开放问题` spec.md §10 O-12 决议内容，本节仅保留轨迹。

### 6.5 038 模块5 `otherPersonnel` 字段名与内容语义错位（迁移 → spec.md §10 O-13）

| 字段       | 值                                                                                                                                                 |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                         |
| 关联位置   | `docs/specs/038-bigdata-personnel-display/spec.md#L304` spec.md §3.3 /preferential L304                                                            |
| 原问题     | §5.3 `otherPersonnel` 标签"其他人员"与 3 字段和（`yfscybyrymbmg + yfscrmjc + yfygscry` 全是伤残类）语义错位；与 `disabledVeteran` 重叠             |
| 决议       | 待 PM 决策：① 改名 `otherDisabled` ② 拆 3 项独立展示 ③ 保留 + 加注释。详见 `docs/specs/038-bigdata-personnel-display/spec.md#10-开放问题` §10 O-13 |
| 待办       | O-13 PM 决议后回填 spec.md §3.3 /preferential L304 标签 + data-model-extensions.md §2.5 SQL 段 + acceptance-tests.md                               |
| 需 PM 决策 | 是（O-13）                                                                                                                                         |
| 状态       | ✅ 已迁移至 spec.md §10 O-13（开放性问题，待 PM 决策）                                                                                             |

> **备注**：按工作分工，开放性问题归 `spec.md §10`，review 问题归 `task-040 §5`。与 §6.4 / O-12 同源（PM 需求说明书 §F.5 模块5 字段定义内部不一致），建议同一 PM 议题讨论，但独立决议。

### 6.6 039 data-models.md §7.3 + §7.5 章节标题 + 字段注释错位（迁移 → spec.md §10 O-20）

| 字段       | 值                                                                                                                                                                                                                                                                                         |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 发现时间   | 2026-08-14                                                                                                                                                                                                                                                                                 |
| 关联位置   | `docs/design/data-models.md#L1335` data-models.md §7.3 L1335-1357 + `docs/design/data-models.md#L1377` §7.5 L1377-1394                                                                                                                                                                     |
| 原问题     | §5.7 + §5.16 合并：① §7.3 letter_screen_3 标题"信访大屏3-人员类型统计"实际用于模块6 /identity，应为"信访大屏6-身份类型分析"；② §7.5 letter_screen_6 标题"信访大屏6-诉求类型统计"应为"信访大屏3-概要统计"；③ 字段注释全部按 PM §4.3 / §4.6 修正（如 JTWX="家庭问题-退伍"→"昨日网上信访量"） |
| 决议       | 待 PM 决策：详见 `docs/specs/039-bigdata-petition-display/spec.md#10-开放问题` §10 O-20                                                                                                                                                                                                    |
| 待办       | O-20 PM 决议后回填 data-models.md §7.3 + §7.5 章节标题 + 字段注释 + acceptance-tests.md（如有 §7.3 / §7.5 断言）                                                                                                                                                                           |
| 需 PM 决策 | 否（PM §4.3 / §4.6 字段映射表为权威）                                                                                                                                                                                                                                                      |
| 状态       | ✅ 已迁移至 spec.md §10 O-20（开放性问题，阻塞高）                                                                                                                                                                                                                                         |

> **备注**：按工作分工，开放性问题归 `spec.md §10`，review 问题归 `task-040 §5`；§7.3 GB_JXGB 与 O-19 关联（待 O-19 决议）；与 §6.7 / O-21 合并处理（章节重编号 + §7.4 新增）。

### 6.7 039 data-models.md §7 缺 letter_screen_4 章节 + 章节编号整体错位（迁移 → spec.md §10 O-21，承接原 O-2）

| 字段       | 值                                                                                                                                                                                                                                                                                                                                                                              |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 发现时间   | 2026-08-14                                                                                                                                                                                                                                                                                                                                                                      |
| 关联位置   | `docs/design/data-models.md` §7（整章节）                                                                                                                                                                                                                                                                                                                                       |
| 原问题     | §5.10 + §5.13 合并：① §7 整文件**无 `letter_screen_4` 章节**（spec.md §3.3 /map + PM §4.4 均引用）；② §7 编号错位（§7.4 实际是 letter_screen_5 → 重编号后应为 §7.5；§7.5 是 letter_screen_6 → §7.6）；③ §7.4 letter_screen_4 字段表按 PM §4.4：主键 + `CANT_NAME` + `CANT_CODE` + `NUM` + `ORGAN_ID` + `REG_AUTHORITY` + `STATS_DATE`（`MAX_TIME` 是否含需 PM 与上游 DDL 对齐） |
| 决议       | 待 PM 决策：详见 `docs/specs/039-bigdata-petition-display/spec.md#10-开放问题` spec.md §10 O-21（原 O-2 已关闭，由 O-21 取代）                                                                                                                                                                                                                                                  |
| 待办       | O-21 PM 决议后回填 data-models.md §7 全部章节重排 + 新增 §7.4 letter_screen_4 + acceptance-tests.md                                                                                                                                                                                                                                                                             |
| 需 PM 决策 | 是（O-21；`letter_screen_4` 是否含 `MAX_TIME` 需 PM 与上游 DDL 对齐）                                                                                                                                                                                                                                                                                                           |
| 状态       | ✅ 已迁移至 spec.md §10 O-21（开放性问题，阻塞高）                                                                                                                                                                                                                                                                                                                              |

> **备注**：按工作分工，开放性问题归 `spec.md §10`，review 问题归 `task-040 §5`；与 §6.6 / O-20 合并处理；承接原 O-2（O-2 描述基于错误假设，已关闭）。

---

## 7. 开放问题汇总（spec.md §10），已在spec中

> 本节为 §3.5 关联开放问题清单；具体讨论在对应 spec.md §10。

| 编号 | 范围 | 标题                                            | 关联          |
| ---- | ---- | ----------------------------------------------- | ------------- |
| O-1  | 038  | `total` 取 `lxgb` / `yfdxhj` / `tyjryfdxhj`     | 模块5 / 模块1 |
| O-11 | 038  | 模块5 `total` 拆分                              | §6.1          |
| O-12 | 038  | 三属 6 字段和（2026-08-17 决议：方案 A）        | §5.1          |
| O-13 | 038  | `otherPersonnel` 改名 / 拆 3 项（待 PM 决策）   | §5.3          |
| O-14 | 039  | /appeal SQL 缺 `STATS_DATE` 取数策略            | §5.5 → §6.3   |
| O-15 | 039  | /appeal `key` 是否含 `appealIdx` 后缀           | §3.3 模块2    |
| O-16 | 039  | /overview API 8 项 vs UI 3 项展示               | §3.3 模块3    |
| O-17 | 039  | /map `cantType` 参数用途                        | §3.3 模块4    |
| O-19 | 039  | /identity `GB_JXGB` "离休" vs PM"军休" 语义分歧 | §5.16         |

---

## 8. 状态记录

| 日期       | 变更                                                                                                                                                                                                                                                                                                                     |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 2026-08-14 | task 创建；模块2 review 完成（无问题）                                                                                                                                                                                                                                                                                   |
| 2026-08-14 | 模块5 review 发现 §3.1（§5.1）错误                                                                                                                                                                                                                                                                                       |
| 2026-08-14 | 模块2 `/total/detail` §3.3 补 `name` 来源引用；改动在已批提案 `R-DOCS-BIGSCREEN-API-UNIFORM` 范围内                                                                                                                                                                                                                      |
| 2026-08-14 | 模块2 `/total` + `/total/detail` §3.3 补 SQL 引用；方案 A（仅引用 data-model-extensions.md §2.2/§2.3，不复制 SQL）；统一在 `R-DOCS-BIGSCREEN-API-UNIFORM` 范围内                                                                                                                                                         |
| 2026-08-14 | 模块3 `/map` review 完成；修复 `data-model-extensions.md §2.4` 字段表 + SQL + 沈抚合并规则（§4.2 ✅）                                                                                                                                                                                                                    |
| 2026-08-14 | 模块4 `/region` review 完成；§4.3 O-4 决议合并 `/map`+`/region` 为单端点（已采纳，待 L3 提案）                                                                                                                                                                                                                           |
| 2026-08-14 | 模块5 `/preferential` review 完成；§5.1-§5.4 登记 4 个问题；§6.1 total 拆分迁移 O-11                                                                                                                                                                                                                                     |
| 2026-08-14 | §6.1 模块5 `total` 拆分迁移至 spec.md §10 O-11；O-4 状态改为"关闭"并加决议说明                                                                                                                                                                                                                                           |
| 2026-08-14 | 模块6 `/employment` review 完成；§4.4-§4.6 登记 + 全部已采纳                                                                                                                                                                                                                                                             |
| 2026-08-14 | **039 接口 review 启动**：模块1 `/trend` review 完成；§4.7 schema 改造 ✅；`data-model-extensions.md §2.1` 旧结构未同步（待办）                                                                                                                                                                                          |
| 2026-08-14 | 039 模块2 `/appeal` review 完成；§4.8 删 label ✅；§5.5 / §5.6 待采纳；O-14 / O-15 开放                                                                                                                                                                                                                                  |
| 2026-08-14 | 039 模块3 `/overview` review 完成；§5.7（§7.5 字段注释错误**阻塞**）/ §5.8 / §5.9 待采纳；O-16 开放                                                                                                                                                                                                                      |
| 2026-08-14 | 039 模块4 + 模块5 review 完成；§5.10（缺 letter_screen_4 **阻塞**）/ §5.11 / §5.12 / §5.13（**阻塞**）待采纳；O-17 开放；O-18 关闭                                                                                                                                                                                       |
| 2026-08-14 | 039 模块5 `/rank` 删 label（§4.9 ✅）；O-18 关闭；§5.12 移除"依赖 O-18"，独立 sync SQL `MAX_TIME` 列                                                                                                                                                                                                                     |
| 2026-08-14 | 039 模块6 `/identity` review 完成；§4.10 label → type ✅；§5.14 / §5.15 / §5.16 待采纳；O-19 开放                                                                                                                                                                                                                        |
| 2026-08-14 | task 文件结构重组：§3 进度索引 / §4 已采纳 / §5 待采纳 / §6 已迁移 / §7 开放问题 / §8 状态记录                                                                                                                                                                                                                           |
| 2026-08-14 | §5.2 修复：038 `data-model-extensions.md §2.5` 字段表机械同步（与 §4.2 / §4.4 同模式）；改为 4 行 `items[].key/name/value/unit` + 下方「各 `key` 取值规则」表，逐项对齐 spec.md §3.3 /preferential；§4.11 已采纳                                                                                                         |
| 2026-08-14 | §5.5 修复（部分）：039 `data-model-extensions.md §2.2` 字段表机械同步为 4 行 `items[].key/name/value/unit`（对齐 spec.md §3.3 /appeal + §4.8 删 label 决议）；§4.12 部分采纳；SQL `STATS_DATE` 取数策略保留待 O-14 决议（§6.3 已迁移）                                                                                   |
| 2026-08-14 | §5.9 修复：039 `data-model-extensions.md §2.3` 派生卡片表头 / 卡片名机械同步（与 §4.2 / §4.4 / §4.11 同模式）；表头 `派生卡片 / 公式` → `卡片名称 / 取值字段`，卡片名 `yesterdayTotal/yesterdayJtfpc/yesterdayJtfrc` → `昨日信访量/昨日集体访批次/昨日集体访人次`；与 spec.md §3.3 /overview L243-249 对齐；§4.13 已采纳 |
| 2026-08-14 | §5.11 修复：039 `data-model-extensions.md §2.4` 字段表机械同步为 4 行 `items[].key/name/value/unit`（对齐 spec.md §3.3 /map）；新增「特殊规则」引用 §4.1 沈抚合并；§4.14 已采纳                                                                                                                                          |
| 2026-08-14 | §5.12 修复：039 `data-model-extensions.md §2.5` 字段表机械同步为 4 行 `items[].key/name/value/unit` + SQL 段同步删 `MAX_TIME` 列（与 §4.9 删 label 决议一致）；§4.15 已采纳                                                                                                                                              |
| 2026-08-14 | §5.15 修复：039 `data-model-extensions.md §2.6` 字段表机械同步为通用 5 行 `items[].key/name/value/unit/type`（与 §4.10 label → type 改造一致）；下方新增「`name` 取值表」(11 行) +「`type` 取值表」(3 行)；SQL 段不动；文末 O-19 命名分歧显式指向 spec.md §10；§4.16 已采纳                                              |
| 2026-08-14 | §5.4 修复：038 spec.md 模块5 详述 L306 口径行 `SELECT *` 改为 `SELECT <12 字段>` + 引用 `data-model-extensions.md §2.5`（与 §4.11 / L241 /preferential「SQL 引用」模式一致）；§4.17 已采纳                                                                                                                               |
| 2026-08-14 | §5.6 修复：039 spec.md §3.2 索引表 /appeal 行 L97（`items: [..., label]` + `按 ITEM_NAME × REG_MAIN_APPEAL_ONE 聚合`）机械同步为 4 行 `items[].key/name/value/unit` + `N = 实际事项数`（与 §4.8 删 label / §4.12 §2.2 字段表一致）；§4.18 已采纳                                                                         |
| 2026-08-14 | §5.14 修复：039 spec.md §3.2 索引表 /identity 行 L101（`items: [..., label]`）机械同步为 `items: [..., type]`（与 §4.10 label → type 改造 / §4.16 §2.6 字段表 5 行结构一致）；§4.19 已采纳                                                                                                                               |
| 2026-08-14 | §5.8 修复：039 `/overview` 8 项 key 统一 lowercase（方案 A）：`jtJtfpc`→`jtjtfpc`、`jtJtfrc`→`jtjtfrc`；同步 4 处（spec.md §3.3 主表 + 派生表 / data-model-extensions.md §2.3 字段表 + 派生表 / acceptance-tests.md L51-52）；DB 列 `JTJTFPC/JTJTFRC` 不变；§4.20 已采纳                                                 |
| 2026-08-17 | §5.1 决议（PM）：038 模块5 `threeDependents` 4 字段和 → **6 字段和（方案 A）**；`docs/specs/038-bigdata-personnel-display/spec.md#10-开放问题` §10 O-12 已关闭；待 L3 提案 `R-DOCS-038-M5-THREE-DEPENDENTS-FIX` 落地 spec.md §3.3 /preferential L297 公式                                                                |
| 2026-08-17 | §5.3 迁移（PM）：038 模块5 `otherPersonnel` 字段名与内容语义错位 → `docs/specs/038-bigdata-personnel-display/spec.md#10-开放问题` §10 O-13（开放性问题，待 PM 决策 ①改名 ②拆 3 项 ③保留+加注释）                                                                                                                         |
| 2026-08-17 | task 文件结构清理（第二轮）：§3.3 [039] 待办清单清空（§5.7 + §5.16 → §6.6 / §5.10 + §5.13 → §6.7）；§3.4 已迁移清单 5 → 7 条；§5 4 → 0 条（移除 §5.7 / §5.10 / §5.13 / §5.16）；§5.7 / §5.10 / §5.13 / §5.16 字段表 已删除（轨迹沉淀至 spec.md §10 O-20 / O-21）；原 O-2 关闭（描述基于错误假设，由 O-21 取代）          |
| 2026-08-17 | §5.1 / §5.3 详细分析 + 决议记录 / §5.7 / §5.10 / §5.13 / §5.16 字段表 已删除（轨迹沉淀至 spec.md §10 O-12 / O-13 / O-20 / O-21）                                                                                                                                                                                         |
