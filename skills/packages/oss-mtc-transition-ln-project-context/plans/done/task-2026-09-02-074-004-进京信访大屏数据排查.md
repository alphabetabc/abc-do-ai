# Task · 2026-09-02-074-004-进京信访大屏数据排查

> 状态：✅ 完成（2026-09-03 收口：F1–F5 修复完成并验证；PIE_COLORS 扩到 10 色；仅剩 demands NULL 分类行隐患未处理）
> 类型：编码/数据排查（父 task：`plans/task-2026-09-02-074-各模块无数据展示.md` §0.10 子 task 机制）
> 创建：2026-09-03
> 关联依据：用户 2026-09-03「创建 task 004，我们看进京信访信息」

---

## 0. 任务信息

| 项        | 值                                                                                                  |
| --------- | --------------------------------------------------------------------------------------------------- |
| 编号-slug | 2026-09-02-074-004-进京信访大屏数据排查                                                             |
| 任务类型  | 编码/数据排查                                                                                       |
| 影响范围  | 040 进京信访大屏（`/visual/big-screen?menu=beijingPetition`）各模块                                 |
| 验收标准  | TBD（待排查后与用户对齐）                                                                           |
| 关联 spec | `docs/specs/040-bigdata-beijing-petition-display/`（spec.md / data-model-extensions.md / tasks.md） |

---

## 1. 背景

- 父 task task-074「各模块无数据展示」排查推进中：
    - 074-001 本地验证库数据补齐（letter_screen_1 等 5 表）
    - 074-002 038 人员信息大屏（4 代码修复 + 1 数据修复）
    - 074-003 039 辽宁信访大屏（5 项修复 + 快照过滤 10 SQL 改按天，**channels/beijing.sql 同样受益**）
- 用户 2026-09-03 指示：创建子 task 004，处理进京信访信息大屏数据问题，需先读 040 spec 五件套

## 2. 040 大屏与 039 的关系（关键：同表不同 REG_AUTHORITY）

> 040 与 039 结构基本同构，核心差异：**`REG_AUTHORITY = '1'`（进京）vs 039 的 `'2'`（本地）**。SQL 复用同一目录 `backend/app/repositories/sql/visual/big_screen/petition/`（beijing.sql / channels.sql 已按 REG=1 实现）。

**已知继承资产**（074-003 修复，040 直接受益）：

- [x] 快照过滤按天（CAST(STATS_DATE AS DATE)）：beijing.sql / channels.sql / trend.sql / demands.sql / map.sql / region_total.sql 等 10 个 SQL 全部已改
- [x] 沈抚合并前缀匹配（`_merge_shenfu` 按 CANT_CODE 前 4 位，041 同族问题已消）
- [x] channels（REG=1）total 已验证回归：visit=3782 / letter=5293 / online=4151
- [x] beijing 概要（REG=1）4 项 items 正常返回

**待排查继承风险**（039 踩过的坑，040 需对照检查）：

- 身份类型横轴中文名映射（039 §4.1：曾硬编码「身份N」）
- 柱图单位错标「万」（039 §4.3：数据实为件级）
- CANT_CODE 9 位 / ORGAN_ID 6/9 位混合（074-002 / 074-003 反复出现）
- letter_screen_6 昨日字段全 0 / NULL（039 §4.5：本地数据如此，需区分数据问题 vs 代码问题）
- spec O-19「GB_JXGB 离休 vs 军休」语义分歧（040/041 同样涉及）

## 3. 排查记录（2026-09-03 对照检查完成）

**重大发现：040 后端是 039 修复前代码的整目录独立拷贝，§2「继承资产」全部不成立**：

- 040 有自己的 `backend/app/services/visual/big_screen/beijing_petition/service.py` + `repositories/sql/visual/big_screen/beijing_petition/`（10 个 SQL，REG='1'），并非复用 `petition/` 目录
- 074-003 修的 4 类问题在 040 **全部存在**：

| #   | 问题                  | 位置                                                                                                   | 039 教训 |
| --- | --------------------- | ------------------------------------------------------------------------------------------------------ | -------- |
| 1   | 身份类型横轴「身份N」 | beijing_petition/service.py L296（grouped 真实）/ L124（mock）/ L317（旧版），无 `_IDENTITY_CN` 映射表 | §4.1     |
| 2   | 沈抚合并等值比较落空  | beijing_petition/service.py L48 `code == "2115"`（库 CANT_CODE 9 位），且无条件重命名 2104 行          | §4.2     |
| 3   | 柱图单位「万」错标    | beijing-petition/petition-region-bar/index.tsx L130 `unit="万"` + L157 `v/10000`                       | §4.3     |
| 4   | 快照过滤精确到秒      | beijing_petition/ 下全部 10 个 SQL 仍 `STATS_DATE = (SELECT MAX(STATS_DATE)...)`                       | §4.6     |

另有 039 §4.4 同款遗留（用户当时未拍板，本次一并跳过）：demands NULL 分类行、PIE_COLORS 8 色板。

## 3.1 修复小任务清单

- [x] F1 身份类型中文名映射：beijing_petition/service.py 增加 `_IDENTITY_CN`，grouped 真实 + mock + 旧版 identity_types 三处 name 改映射
- [x] F2 沈抚合并：`_merge_shenfu` 改前 4 位前缀匹配 + 仅市级行（余位全 0）重命名「抚顺市」
- [x] F3 柱图单位：petition-region-bar `unit="万"` → `"件"`，`yAxisLabelFormatter` 去掉 `/10000`
- [x] F4 快照过滤按天：beijing_petition/ 10 个 SQL 改 `CAST(STATS_DATE AS DATE) = (SELECT MAX(CAST(STATS_DATE AS DATE)) ...)`，map/region_distribution 保留 `IS NULL OR` 兼容分支
- [x] F5 验证：直调 beijing_petition service 各端点（backend/.venv），临时脚本已删除

## 3.2 修复执行结果（2026-09-03，`_tmp_check_bj.py` 已删）

- identity_grouped：33 项（3 渠道 × 11），names 为 11 个中文名；旧版 identity_types 同样 11 个中文名
- region_total / map：各 14 项，无沈抚（211500000 已并入 210400000），抚顺含合并值
- trend 36 / demands 9 / overview 9 项正常
- channels total：visit=3782 / letter=5293 / online=4151（与 074-003 修复后 petition/channels.sql REG=1 结果一致）；yesterday 全 0——本地数据如此（同 039 §4.5 结论）
- beijing 概要 4 项正常（昨日字段 0 同上）

**遗留**：039 §4.4 同款 UI 隐患（demands NULL 分类行、PIE_COLORS 8 色板）未处理，与 039 保持一致待拍板。

## 4. 状态记录

| 日期       | 变更                                                                                                                                                                                                                                                    |
| ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 2026-09-03 | task 创建：处理 040 进京信访大屏数据问题；已列 040/039 同构关系 + 继承资产 + 继承风险清单；具体症状待用户录入                                                                                                                                           |
| 2026-09-03 | **排查发现 040 后端为 039 修复前代码整目录独立拷贝（beijing_petition/ service + SQL），「继承资产」全部不成立**；执行 F1–F5 修复：身份类型中文名映射、沈抚合并前缀匹配、柱图单位万→件、10 个 SQL 快照过滤改按天；直调 service 全部验证通过（§3 / §3.2） |
| 2026-09-03 | PIE_COLORS 8→10 色（+玫红/青绿），demands 9 项不再撞色（039 §4.4 三号隐患消除）；task 收口：数据排查任务全部结束，仅剩 demands NULL 分类行隐患未处理                                                                                                    |
