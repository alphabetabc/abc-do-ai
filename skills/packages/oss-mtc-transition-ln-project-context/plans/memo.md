# Memo · 大屏可视化分析待办与探索

> 性质：非正式备忘，存放待办、探索中、未立案的想法
> 维护规则：随时记、随时划；正式立案后移入 roadmap §4 或 design/002

---

## 1. 风险与开放问题

| #   | 内容                                                                            | 状态       | 备注                                      |
| --- | ------------------------------------------------------------------------------- | ---------- | ----------------------------------------- |
| R1  | PM HTML 原型（`pm/大屏需求HTML/`）与 88 张设计稿之间未必一致                    | ⚪ 待确认  | 需 PM 二次确认（写进 pm-input §I）        |
| R2  | 二级下钻地图的缩放 / 分辨率策略未拍板                                           | ⚪ 探索中  | AGENTS §5.5、private-notes §2026-08-11    |
| R3  | 沈抚（2115）合并抚顺（2104）的口径在 035 月报已实现，需大屏 SQL 复刻            | ⚪ 待复刻  | 035 已有实现可参考                        |
| R4  | 4 大屏是"平级 4 菜单" 还是 "1 菜单 4 tab" 未拍板                                | 🟡 跟随 D3 | 已纳入 `design/002` D-R4；倾向 4 平级菜单 |
| R5  | `letter_screen_5` 字段（`cant_name` / `letter_count`）与 `abi_wjj` 口径是否一致 | ⚪ 待验证  | 自己翻 dump 数据验证，先别问 PM           |
| R6  | 038 模块5「优抚对象总数」取 `lxgb`（离休干部）字段，语义不符                  | ⚪ 待 PM 确认 | task-015 B3；PM 输入原文 `SELECT lxgb`，但 `lxgb` 是「离休干部」。可不等数据直接问 PM：这个字段对不对？还是该取合计？ |

---

## 2. 探索中的想法

> 以下想法尚未正式立案，仅作记录。成熟后移入 roadmap §4 作为正式 task。

| #   | 想法                                              | 状态      | 备注                                                                                                                                              |
| --- | ------------------------------------------------- | --------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1  | 公共图表组件库是否独立为 `components/big-screen/` | ⚪ 探索中 | 4 spec 共享翻牌/地图/轮播等组件                                                                                                                   |
| M2  | 大屏是否需要 WebSocket 实时刷新                   | ⚪ 待定   | PM 未明确要求；暂按静态快照设计                                                                                                                   |
| M3  | 88 张设计稿是否全迁入 pm-inputs/assets/           | ⚪ 待定   | 跟随 D6                                                                                                                                           |
| M4  | 是否需要大屏专用主题令牌（暗色/高对比）           | ⚪ 待定   | system-overview §2.5 主题令牌待定；分辨率适配思路：`scale` 缩放 + `transform-origin: top left` + `vh/vw`，可参考 `datav-vue3` viewport 方案自实现 |

---

## 3. 待办

| #   | 内容                                                         | 状态    | 备注 |
| --- | ------------------------------------------------------------ | ------- | ---- |
| T1  | 把 `frontend` + `backend` 跑起来，验证本机环境完整           | ⚪ 待做 | -    |
| T2  | 重读 `035-visual-monthly-statistics/spec.md` M4 分析报告范围 | ⚪ 待做 | -    |
| T3  | ec-map `getMapConfig` 地图区域配置数据来源确定              | ✅ 完成 | `frontend/public/static/map/map-config.json`，组件加载后缓存到模块级变量 |
| T4  | ec-map `STATIC_PATH` 常量确定（geojson 静态资源路径）       | ✅ 完成 | `/static`（Vite public 映射）；geojson 路径 `/static/map/geojson/{adcode}.geojson`，geojson 文件本身尚不存在 |

---

## 4. 非大屏范围的发现（记录但不处理）

| #   | 内容                                                                                                                              | 位置            | 处理   |
| --- | --------------------------------------------------------------------------------------------------------------------------------- | --------------- | ------ |
| N1  | `system-overview.md` §2.2.2 中部分比对模块路由与 `specs/index.md` 说明不一致（如 012 `/account/dict` vs `/account/dictionaries`） | system-overview | 不处理 |
| N2  | `api-contracts.md` §7.1 Git 合并冲突标记                                                                                          | L155–L194       | D8     |
