# Task · 038 编码前置阻塞清理

> 状态：⚪ 待启动
> 关联：`plans/roadmap-2026-08-11-big-screen.md` §2 M1-M5
> 类型：调研 + 资源获取 + 口径确认
> 创建：2026-08-12
> 前置：
>
> - task-013（✅ 038 五件套已生成）
> - task-014（✅ M0 文档审批已完成）
> - 阻塞对象：task-016（038 编码开发 M1-M5）

---

## 0. 任务信息

| 项        | 值                                                                      |
| --------- | ----------------------------------------------------------------------- |
| 编号-slug | 038-bigdata-personnel-display                                           |
| spec 目录 | `docs/specs/038-bigdata-personnel-display/`                             |
| 阻塞目标  | task-016（编码开发 M1-M5）                                              |
| 关联 spec | [spec.md](docs/specs/038-bigdata-personnel-display/spec.md) §9 开放问题 |

---

## 1. 阻塞项清单

> 每项标注：来源 / 阻塞哪个里程碑 / 谁能解除 / 处理方式

### B1 — 数据库可读性确认（阻塞 M1）

- **来源**：[plan.md](docs/specs/038-bigdata-personnel-display/plan.md) §2 前置
- **内容**：确认 Kingbase 以下三张表可读 + 有测试数据
    - `dw_basic_lc.stats_jdlk_persion_infor_cant`（模块1/2/3/4）
    - `dw_basic_lc.abi_zb_tyjrjqyfdx`（模块2下钻/模块5）
    - `dw_basic_lc.aa_jycy`（模块6）⚠️ schema 已更正：原 `dw_archive` 是错误的，实际在 `dw_basic_lc`（来源：`aa_jycy.sql` 文件头 `Source Schema: dw_basic_lc`）
- **处理方式**：连数据库执行 `SELECT 1 FROM <table> LIMIT 1`；若无测试数据需协调导入
- **🛑 等待用户**：可能需要提供数据库连接或确认环境

#### B1 验证 SQL（Kingbase 可直接执行）

```sql
-- ① 三张表可读性 + 行数
SELECT 'stats_jdlk_persion_infor_cant' AS tbl, COUNT(*) AS cnt
FROM dw_basic_lc.stats_jdlk_persion_infor_cant
UNION ALL
SELECT 'abi_zb_tyjrjqyfdx', COUNT(*)
FROM dw_basic_lc.abi_zb_tyjrjqyfdx
UNION ALL
SELECT 'aa_jycy', COUNT(*)
FROM dw_basic_lc.aa_jycy;

-- ② stats_jdlk_persion_infor_cant 省级数据样本（模块1/2/3/4 数据源）
SELECT PROVINCE_CODE, CANT_TYPE, STATS_UNIT_NAME,
       VETERAN, OFFICER, SERGEANT, CONSCRIPTS, CAT_PRE_OBJECT,
       STATS_DATE
FROM dw_basic_lc.stats_jdlk_persion_infor_cant
WHERE PROVINCE_CODE = '21'
LIMIT 5;

-- ③ abi_zb_tyjrjqyfdx 省级数据样本（模块2下钻/模块5 数据源）
SELECT cant_code, lxgb, txjg, zyjg, zzzyjzgb, qyjzgb,
       yftycjjr, yfzxfyjr, yfdbhxtyjr, yfnmlszsncjtysb,
       yfscybyrymbmg, yfscrmjc, yfygscry,
       insert_time
FROM dw_basic_lc.abi_zb_tyjrjqyfdx
WHERE cant_code = '21'
LIMIT 5;

-- ④ 各表的 CANT_TYPE / cant_code 分布（确认有市级/区县级数据）
SELECT CANT_TYPE, COUNT(*) AS cnt
FROM dw_basic_lc.stats_jdlk_persion_infor_cant
GROUP BY CANT_TYPE
ORDER BY CANT_TYPE;

SELECT cant_code, COUNT(*) AS cnt
FROM dw_basic_lc.abi_zb_tyjrjqyfdx
GROUP BY cant_code
ORDER BY cant_code
LIMIT 20;
```

#### `aa_jycy` 建表 SQL（MySQL 方言，需转 Kingbase）

> 源库：MySQL 5.7.27，Schema `dw_basic_lc`，表 `aa_jycy`
> ⚠️ 这是 MySQL 语法，Kingbase 需转换：反引号→双引号、去掉 `ENGINE/CHARACTER SET/COLLATE/ROW_FORMAT`、`utf8mb4`→`utf8`

```sql
-- Kingbase 可执行版本（已转换：反引号→双引号、去掉 MySQL 特有子句）
DROP TABLE IF EXISTS "aa_jycy";
CREATE TABLE "aa_jycy" (
  "序号" varchar(255) NULL DEFAULT NULL,
  "姓名" varchar(255) NULL DEFAULT NULL,
  "身份证号" varchar(255) NULL DEFAULT NULL,
  "安置地" varchar(255) NULL DEFAULT NULL,
  "rylb" varchar(255) NULL DEFAULT NULL,
  "联系方式" varchar(255) NULL DEFAULT NULL,
  "学历" varchar(255) NULL DEFAULT NULL,
  "专业" varchar(255) NULL DEFAULT NULL,
  "退役年份" varchar(255) NULL DEFAULT NULL,
  "是否企业参保" varchar(255) NULL DEFAULT NULL,
  "企业参保状态" varchar(255) NULL DEFAULT NULL,
  "是否机关和事业单位参保" varchar(255) NULL DEFAULT NULL,
  "机关和事业单位参保状态" varchar(255) NULL DEFAULT NULL,
  "是否居民参保" varchar(255) NULL DEFAULT NULL,
  "居民参保状态" varchar(255) NULL DEFAULT NULL,
  "cbzt" varchar(255) NULL DEFAULT NULL,
  "招录性质" varchar(255) NULL DEFAULT NULL,
  "参保单位" varchar(255) NULL DEFAULT NULL,
  "岗位名称" varchar(255) NULL DEFAULT NULL,
  "未参保原因" varchar(255) NULL DEFAULT NULL,
  "选择原因" varchar(255) NULL DEFAULT NULL,
  "学校名称" varchar(255) NULL DEFAULT NULL,
  "专业名称" varchar(255) NULL DEFAULT NULL,
  "参保原因" varchar(255) NULL DEFAULT NULL,
  "提供就业服务情况" varchar(255) NULL DEFAULT NULL,
  "岗位1" varchar(255) NULL DEFAULT NULL,
  "岗位2" varchar(255) NULL DEFAULT NULL,
  "岗位3" varchar(255) NULL DEFAULT NULL,
  "是否参加“以工代赈”行动" varchar(255) NULL DEFAULT NULL,
  "填报人" varchar(255) NULL DEFAULT NULL,
  "填报人联系方式" varchar(255) NULL DEFAULT NULL,
  "状态" varchar(255) NULL DEFAULT NULL
);
CREATE INDEX "idx_aa_jycy_身份证号" ON "aa_jycy" ("身份证号");
```

**字段清单（32 列，大段中文字段名）**：
`序号` / `姓名` / `身份证号` / `安置地` / `rylb` / `联系方式` / `学历` / `专业` / `退役年份` / `是否企业参保` / `企业参保状态` / `是否机关和事业单位参保` / `机关和事业单位参保状态` / `是否居民参保` / `居民参保状态` / `cbzt` / `招录性质` / `参保单位` / `岗位名称` / `未参保原因` / `选择原因` / `学校名称` / `专业名称` / `参保原因` / `提供就业服务情况` / `岗位1` / `岗位2` / `岗位3` / `是否参加"以工代赈"行动` / `填报人` / `填报人联系方式` / `状态`

**spec/PM 用到的字段**：`rylb`（人员类别，英文字段）、`cbzt`（参保状态，英文字段）、`安置地`（地市级筛选，中文字段）

> ⚠️ 此 SQL 只有表结构，**没有 INSERT 数据**。建完表是空的，`rylb` 枚举值和「安置地」存储格式仍需样本数据确认（见 B2）。

### B2 — `aa_jycy` 表字段与枚举确认（阻塞 M2 模块6）

- **来源**：spec §9 O-2 / O-5 / O-6；[data-model-extensions.md](docs/specs/038-bigdata-personnel-display/data-model-extensions.md) §2.6
- **待确认**：
    - **O-2**：中文字段名（「安置地」）在 Kingbase SQL 中是否可用双引号包裹
    - **O-5**：`rylb` 字段的实际枚举值（需过滤出 PM 要求的 4 类人员：自主就业退役义务兵/自主就业退役军士/自主择业军转干部/逐月领取退役金）
    - **O-6**：「安置地」字段存储的是地市名称还是编码（决定地市级筛选 SQL 写法）
- **处理方式**：
    1. `SELECT DISTINCT rylb FROM dw_basic_lc.aa_jycy` —— 确认枚举值
    2. `SELECT "安置地" FROM dw_basic_lc.aa_jycy LIMIT 10` —— 确认存储格式
    3. `SELECT rylb, "安置地", cbzt FROM dw_basic_lc.aa_jycy LIMIT 10` —— 确认中文列名可用性
- **🛑 等待用户**：需要数据库访问权限

#### B2 验证 SQL（Kingbase 可直接执行）

```sql
-- ① O-5：rylb 枚举值（PM 要求过滤 4 类人员，需看实际值）
SELECT DISTINCT rylb, COUNT(*) AS cnt
FROM dw_basic_lc.aa_jycy
GROUP BY rylb
ORDER BY rylb;

-- ② O-6：安置地存储格式（名称 or 编码？）
SELECT DISTINCT "安置地"
FROM dw_basic_lc.aa_jycy
ORDER BY "安置地"
LIMIT 20;

-- ③ O-2：中文列名兼容性 + 数据样本（三条一起验证）
SELECT rylb, cbzt, "安置地", "姓名", "身份证号"
FROM dw_basic_lc.aa_jycy
LIMIT 10;

-- ④ 如果 ②③ 报错，试不加双引号的（Kingbase 大小写不敏感时可能直接可用）
-- SELECT DISTINCT 安置地 FROM dw_basic_lc.aa_jycy LIMIT 20;
-- SELECT rylb, cbzt, 安置地 FROM dw_basic_lc.aa_jycy LIMIT 10;
```

### B3 — 模块5「优抚对象总数」口径确认（阻塞 M2 模块5）

- **来源**：spec §9 O-1；[data-model-extensions.md](docs/specs/038-bigdata-personnel-display/data-model-extensions.md) §2.5
- **问题**：PM 输入原文是 `SELECT lxgb FROM abi_zb_tyjrjqyfdx`，但 `lxgb` 是「离休干部」字段，与「优抚对象总数」语义不符
- **候选方案**：
    - 方案 A：按 PM 原文取 `lxgb`（当前 spec 写法）
    - 方案 B：改取 `tyjryfdxhj`（退役军人及优抚对象合计）或其他合计字段
- **处理方式**：找 PM 确认；确认后回填 spec §9 O-1 + data-model-extensions §2.5
- **🛑 等待用户**：需找产品经理确认

#### B3 验证 SQL（给 PM 提供决策依据）

```sql
-- ① 对比 lxgb（离休干部）与模块5 各分项字段，看哪个更像「优抚对象总数」
SELECT cant_code,
       lxgb        AS "离休干部_PM原文取这个",
       yftycjjr    AS "退役残疾军人",
       yfzxfyjr    AS "在乡复员军人",
       yfdbhxtyjr  AS "带病回乡退役军人",
       yfnmlszsncjtysb AS "年满60农村籍退役士兵",
       (yfxsgjdqfxlsys + yfxsgjdqbzlslnzn + yfxsgjdqfxygxsjrys + yfxsgjdqfxbgjrys) AS "三属合计",
       (yfscybyrymbmg + yfscrmjc + yfygscry) AS "其他人员合计",
       -- 六项分项合计（可能才是真正的「优抚对象总数」）
       (yftycjjr + yfzxfyjr + yfdbhxtyjr + yfnmlszsncjtysb
        + yfxsgjdqfxlsys + yfxsgjdqbzlslnzn + yfxsgjdqfxygxsjrys + yfxsgjdqfxbgjrys
        + yfscybyrymbmg + yfscrmjc + yfygscry) AS "六项分项合计"
FROM dw_basic_lc.abi_zb_tyjrjqyfdx
WHERE cant_code = '21';

-- ② 如果表里有 tyjryfdxhj（退役军人及优抚对象合计）字段，也一起对比
-- SELECT cant_code, lxgb, tyjryfdxhj,
--        (yftycjjr + yfzxfyjr + yfdbhxtyjr + yfnmlszsncjtysb
--         + yfxsgjdqfxlsys + yfxsgjdqbzlslnzn + yfxsgjdqfxygxsjrys + yfxsgjdqfxbgjrys
--         + yfscybyrymbmg + yfscrmjc + yfygscry) AS "六项分项合计"
-- FROM dw_basic_lc.abi_zb_tyjrjqyfdx
-- WHERE cant_code = '21';
```

> 拿到结果后找 PM 确认：「优抚对象总数」到底取 `lxgb`，还是六项分项合计，还是其他字段。

#### B1-B3 验证结论（2026-08-12）

| 项                       | 结论                                                                  | 状态                            |
| ------------------------ | --------------------------------------------------------------------- | ------------------------------- |
| **B1** 表可读性          | 三张表 SQL 均不报错，表结构可用；但**表中无数据**（大部分查询返回空） | ⚠️ 结构可用，待数据导入         |
| **B1** `aa_jycy` 建表    | Kingbase 建表 SQL 执行成功（双引号包裹中文字段名）                    | ✅ 已建表                       |
| **B2-O-2** 中文字段名    | Kingbase 支持双引号包裹中文字段名，SQL 不报错                         | ✅ 已确认（无数据，仅验证语法） |
| **B2-O-5** `rylb` 枚举值 | SQL 不报错但**无数据返回**，枚举值未知                                | ⚠️ 待数据导入后确认             |
| **B2-O-6** 安置地格式    | SQL 不报错但**无数据返回**，存储格式未知                              | ⚠️ 待数据导入后确认             |
| **B3** `lxgb` 口径对比   | SQL 不报错但**无数据返回**，无法对比                                  | ⚠️ 待数据导入后确认             |

> **总结**：B1-B3 的 SQL 语法验证全部通过（表结构 + Kingbase 方言兼容性 OK），但**数据未导入**导致业务层面开放问题（O-1/O-5/O-6）无法关闭。数据导入由用户自行处理，不阻塞当前 task 推进。

### B4 — geojson 文件获取（阻塞 M3 地图模块）

- **来源**：[plan.md](docs/specs/038-bigdata-personnel-display/plan.md) §2；memo T4
- **内容**：辽宁省/各地市/各区县 geojson 文件
- **目标路径**：`frontend/public/static/map/geojson/{adcode}.geojson`
- **当前状态**：路径约定已确定（memo T4 ✅），但 **geojson 文件本身不存在**
- **处理方式**：
    1. 确认是否有内部地图资源库
    2. 从公开数据源获取（如 aliyun DataV geojson）
    3. 放入 `frontend/public/static/map/geojson/`
- **🛑 等待用户**：确认数据来源

### B5 — PM 附件补充（阻塞 M5 联调对数）

- **来源**：spec 附录 A
- **缺失文件**：
    - 截图 1：省级视图 `pm-inputs/assets/image_20260525171204501.png`
    - 截图 2：市级视图 `pm-inputs/assets/image_20260525171506159.png`
    - 行政区划字典说明.xlsx `pm-inputs/assets/行政区划字典说明.xlsx`
- **影响**：
    - 截图缺失 → M5 无法与 PM 截图对数
    - xlsx 缺失 → §4.5 行政区划编码不完整（目前只有 21/2101/2102/2115 几个示例）
- **处理方式**：找 PM 补充附件
- **🛑 等待用户**：需产品经理提供

### B6 — 导航按钮跨 spec 路由回填（阻塞 M5 导航，低优）

- **来源**：spec §9 O-3；spec §8.2 上游回填清单
- **问题**：039/040/041 spec 未创建，导航按钮点击行为待定
- **当前处理**：spec §4.3 已约定「未建大屏按钮点击后 toast 提示『大屏建设中』」
- **优先级**：低（不阻塞 M1-M4，仅 M5 导航按钮行为）
- **处理方式**：039-041 spec 创建后回填路由；本期按 toast 提示实现

---

## 2. 执行步骤

> 按优先级排序；每项完成后回填 spec §9 对应开放问题状态

### 步骤 1：数据库可读性 + `aa_jycy` 字段确认（B1 + B2）

- **动作**：连数据库执行验证 SQL（见 B1/B2 处理方式）
- **输出**：
    - 三张表可读性结论
    - `aa_jycy.rylb` 枚举值清单
    - `aa_jycy.安置地` 存储格式结论
    - 中文字段名 Kingbase SQL 兼容性结论
- **回填**：spec §9 O-2 / O-5 / O-6 状态改为「已确认」并写结论
- **🛑 等待用户**：是（需要数据库访问）

### 步骤 2：模块5 口径确认（B3）

- **动作**：找 PM 确认「优抚对象总数」取 `lxgb` 还是其他字段
- **输出**：PM 确认结论
- **回填**：spec §9 O-1 状态改为「已确认」；data-model-extensions §2.5 按结论更新
- **🛑 等待用户**：是（需找产品经理）

### 步骤 3：geojson 文件获取（B4）

- **动作**：获取辽宁省/各地市/各区县 geojson 文件，放入 `frontend/public/static/map/geojson/`
- **输出**：geojson 文件清单
- **🛑 等待用户**：是（确认数据来源）

### 步骤 4：PM 附件补充（B5）

- **动作**：找 PM 补充截图 + 行政区划 xlsx
- **输出**：附件文件放入 `pm-inputs/assets/`
- **🛑 等待用户**：是（需产品经理提供）

### 步骤 5：低优项登记（B6）

- **动作**：确认 039-041 创建计划；本期按 toast 提示实现
- **输出**：spec §9 O-3 备注更新
- **🛑 等待用户**：否

---

## 3. 验收

- [x] B1：三张表可读性确认，有测试数据（或已协调导入计划）—— ⚠️ 表结构可用，数据待用户导入
- [x] B2：`aa_jycy` 的 `rylb` 枚举值 + 「安置地」格式 + 中文字段名兼容性已确认 —— ✅ 语法兼容性已确认；枚举值/格式待数据导入
- [x] B3：模块5「优抚对象总数」口径已确认 —— ⚠️ SQL 可执行，待数据导入后给 PM 对比
- [x] B4：geojson 文件已就位 —— ✅ 115 个文件（1 省 + 14 市 + 100 区县）下载到 `frontend/public/static/map/geojson/`
- [ ] B5：PM 附件已补充（或已明确无法提供，M5 联调降级方案）
- [ ] B6：导航按钮行为已登记（本期 toast 提示）
- [ ] spec §9 O-1~O-6 状态已回填 —— O-2 ✅；O-1/O-5/O-6 待数据导入
- [ ] task-016 §1 前置条件可勾选

---

## 4. 状态记录

| 日期       | 变更                                                                                                                                                                                                                         |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| 2026-08-12 | 原 task-015（编码开发）改为 task-016；本 task 重新定义为「038 编码前置阻塞清理」（B1-B6），等待用户授权启动数据库验证                                                                                                        |
| 2026-08-12 | 阻塞项清单梳理完成（B1-B6），等待用户授权启动数据库验证                                                                                                                                                                      |
| 2026-08-12 | B1/B2 schema 更正：`dw_archive.aa_jycy` → `dw_basic_lc.aa_jycy`（依据：`aa_jycy.sql` 文件头 `Source Schema: dw_basic_lc`）；B1 追加建表 SQL（MySQL 方言，需转 Kingbase）；同步修正 data-models.md §8.7 + 038 五件套 6 处引用 |                              |
| 2026-08-12 | B1-B3 验证 SQL 执行完毕：① `aa_jycy` 建表成功（Kingbase 双引号语法 OK）；② 三张表 SQL 均不报错；③ O-2 中文字段名兼容性已确认 ✅；④ 表中无数据，O-1/O-5/O-6 待数据导入后确认；B1-B3 验收勾选（附备注）                        | 语法验证通过，业务验证待数据 |
| 2026-08-12 | B4 完成：geojson 批量下载脚本编写 + 执行成功，115 个文件（1 省 + 14 市 + 100 区县）下载到 `frontend/public/static/map/geojson/`；脚本存放 `scripts/download-liaoning-geojson.mjs`；修复默认输出路径计算错误                  | B4 ✅ 完成                   |
