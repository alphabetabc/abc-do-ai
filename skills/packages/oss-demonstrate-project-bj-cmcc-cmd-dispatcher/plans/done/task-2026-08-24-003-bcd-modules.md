# Task 003 — B/C/D 组模块图片 + 命名二次确认

> 文档定位：补充 task001 阶段未到位的模块图片资源 + 处理 task001 §9.5 甩过来的命名二次确认。
> 前置：
>
> -   [done/task-2026-08-24-001-module-init.md](./done/task-2026-08-24-001-module-init.md) 骨架已落地
> -   [task-2026-08-24-002-map.md](./task-2026-08-24-002-map.md) 已完成 PM 重构
>     关联文档：
> -   设计稿：[../design/001-pm-output.md](../design/001-pm-output.md)
> -   前端规范：[../design/003-frontend.md](../design/003-frontend.md)
> -   当前状态：[../status/current.md](../status/current.md)
>
> 日期：2026-08-24
> 状态：✅ 图片接入已完成（与 task002-01 同窗口落地）；PM 命名二次确认待定

---

## 一、目标

1. **B/C/D 组模块图片补齐**（task001 阶段仅 A 组 4 张图到位）
2. **PM 二次确认模块命名**（task001 §9.5 甩过来的 #5 项）
3. 同步更新 task002 已重构的 `MY_GROUPS`（命名调整后，模块归属要再校验一次）

---

## 二、PM 命名二次确认（task001 §9.5 #5）

当前 task001 落地的模块中文名，PM 在 task001 §9.1 回复后可能需要调整：

| 当前命名                                  | 是否改名  | PM 候选名（待二次确认）                          | 备注                             |
| ----------------------------------------- | --------- | ------------------------------------------------ | -------------------------------- |
| 退服小区恢复情况                          | ⚠️ 需确认 | "退服站点恢复情况" / "退服恢复情况（小）" / 不改 | 无"小区"层级后，这个名字有点歧义 |
| 小区清单                                  | ⚠️ 需确认 | "逻辑站清单" / "物理站清单" / 不改               | 同上                             |
| 站址画像                                  | ⚠️ 需确认 | "逻辑站画像" / 不改                              | 与"物理站 vs 逻辑站"语义冲突     |
| 站址性能                                  | ⚠️ 需确认 | "逻辑站性能" / 不改                              | 同上                             |
| 基站退服                                  | ✅ 不变   | —                                                | 与"物理站"语义一致               |
| 网络影响                                  | ✅ 不变   | —                                                | —                                |
| 退服恢复情况                              | ✅ 不变   | —                                                | —                                |
| 指标 / 联保障任务 / 告警明细 / 逻辑站清单 | ✅ 不变   | —                                                | 通用名，无歧义                   |

> ⚠️ **已取消**：PM 二次确认不再推进，4 个 ⚠️ 命名保持现状（退服小区恢复情况 / 小区清单 / 站址画像 / 站址性能 均不改名）。

---

## 三、B/C/D 组模块图片接入清单

### 3.1 当前已就位（task001 + task002-01 同窗口落地）

| 模块                                                           | 图                                      | 状态                                    |
| -------------------------------------------------------------- | --------------------------------------- | --------------------------------------- |
| 地图（city/company/district/street/community/station/logical） | `map/${level}/base.png` + `outline.png` | ✅ 已接（7 层级全建，暂用 city 图占位） |
| 网络影响（A/B）                                                | `网络影响.png` + `-2/3/4/5.png`         | ✅ 已接（5 层级各一张，getter 映射）    |
| 基站退服（A/B）                                                | `基站退服.png`                          | ✅ 已接                                 |
| 退服恢复情况（A）                                              | `退服恢复情况.png`                      | ✅ 已接                                 |
| 退服小区恢复情况（B/C/D）                                      | `退服小区恢复情况.png`                  | ✅ 已接（task002-01 窗口补传 src）      |
| 指标（C）                                                      | `指标.png`                              | ✅ 已接（占位 div → `<img>`）           |
| 逻辑站清单（C）                                                | `逻辑站清单.png`                        | ✅ 已接                                 |
| 联保障任务（C）                                                | `联保障任务.png`                        | ✅ 已接                                 |
| 告警明细（C）                                                  | `告警明细.png`                          | ✅ 已接                                 |
| 站址画像（D）                                                  | `站址画像.png`                          | ✅ 已接                                 |
| 小区清单（D）                                                  | `小区清单.png`                          | ✅ 已接                                 |
| 站址性能（D）                                                  | `站址性能.png`                          | ✅ 已接                                 |

### 3.2 ~~待补（B/C/D 组，task001 留占位）~~ → 已全部完成

> 全部模块图片已在 task002-01 窗口接入，无待补项。

### 3.3 接入方式（参考 task001 既有模式）

-   **图片型**（如 站址画像 / 小区清单 / 站址性能）：直接 `<img src="/static/images/bj-cmcc-cmd-dispatcher/<name>.png">`>
-   **结构型**（如 指标 / 联保障任务 / 告警明细 / 逻辑站清单）：本次仍**仅占位 div**（不接业务数据），等数据来源确认后再做

---

## 四、落地方案

### 4.1 文件改动

| 文件                                          | 改动                                                                                                           |
| --------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `modules/network-impact/index.tsx`            | ✅ 已完成：按层级切换图片，`Record<Level, { get src(): string }>` getter 映射，路径基于 `constants.IMAGE_PATH` |
| `modules/service-recovery-cell/index.tsx`     | 接入 `退服小区恢复情况.png`（如 PM 不改名） 或改名后对应图片                                                   |
| `modules/indicators/index.tsx`                | 占位保留，不接图                                                                                               |
| `modules/logical-station-list/index.tsx`      | 接入图（如有） 或 改名为 D 组模块                                                                              |
| `modules/support-tasks/index.tsx`             | 占位保留                                                                                                       |
| `modules/alarm-detail/index.tsx`              | 占位保留                                                                                                       |
| `modules/station-portrait/index.tsx`          | 接入图（命名 / 图片待 PM）                                                                                     |
| `modules/cell-list/index.tsx`                 | 接入图（命名 / 图片待 PM）                                                                                     |
| `modules/station-performance/index.tsx`       | 接入图（命名 / 图片待 PM）                                                                                     |
| `components/service-recovery-panel/index.tsx` | 命名若改，组件名 / variant 也要同步改                                                                          |
| `store/index.ts`                              | 若命名改了，模块归属与 Group 划分要再校验                                                                      |

### 4.2 不同层级截图差异

部分模块在不同层级下截图内容不同，需要按层级切换图片：

| 模块             | 层级差异                                                         | 处理方式                                                                                                                                                                                                                  |
| ---------------- | ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 基站退服         | A 组（city/company/district/street）与 B 组（community）截图不同 | 按 `currentLevel` 切换 `<img src>`                                                                                                                                                                                        |
| 网络影响         | A 组与 B 组截图不同 → 实际 5 层级各不相同                        | ✅ 已完成：`Record<Level, { get src(): string }>` getter 映射（city→网络影响.png / company→网络影响-2.png / district→网络影响-3.png / street→网络影响-4.png / community→网络影响-5.png），路径基于 `constants.IMAGE_PATH` |
| 退服小区恢复情况 | B/C/D 组各层级截图可能不同                                       | 待 PM 确认后按层级切换                                                                                                                                                                                                    |

> **实现参考**：类似 `MAP_LEVEL_ASSETS` 的 getter 模式，或 `Record<Level, string>` 映射，在组件内根据 `currentLevel` 取对应图片。

### 4.3 基站退服数据支持

基站退服模块当前仅展示静态图片，后续需要接入真实数据：

| 数据项       | 说明                                   | 数据来源               |
| ------------ | -------------------------------------- | ---------------------- |
| 退服基站列表 | 基站名称、退服时间、退服原因、影响范围 | fetch / socket（待定） |
| 退服数量统计 | 总数、今日新增、已恢复                 | 同上                   |
| 趋势图       | 近 7 天退服趋势                        | 同上                   |

> **前置依赖**：需要后端提供接口或 mock 数据，当前阶段仅占位 `<img>`，数据接入在后续 task 中处理。

### 4.4 不在本次范围

-   ❌ 业务数据接入（fetch / socket）
-   ❌ 图表渲染（echarts 等）
-   ❌ 表格 / 列表实现（仅占位）
-   ❌ 退服小区恢复情况 cell 层重构（task002 已处理 currentLevel 删除 cell，本 task 不再讨论）

---

## 五、看板

-   [x] ~~PM 二次确认 4 个模块的最终命名（§2）~~ → 已取消：不再确认，4 个命名保持现状
-   [x] ~~资源就位后逐模块接入 `<img>`（§3.2）~~ → 全部 8 个模块图片已接入（task002-01 窗口完成）
-   [x] ~~结构型模块占位保留~~ → 改为全部用 `<img>` 引入图片（用户已提供全部图片）
-   [x] ~~模块归属校验 `MY_GROUPS`~~ → 已删除 Group 抽象，改为 `MY_LEVELS: Level[]` 直接声明可见层级（task002-01 窗口完成）
-   [x] 全部模块删除 `pointerEvents: 'none'`
-   [x] TS 编译通过
-   [x] ~~按层级切换截图图片（§4.2：基站退服 / 网络影响 / 退服小区恢复情况）~~ → 网络影响已完成（`Record<Level, { get src(): string }>` getter 映射，city→网络影响.png / company→网络影响-2.png / district→网络影响-3.png / street→网络影响-4.png / community→网络影响-5.png）；基站退服 / 退服小区恢复情况待后续 task
-   [ ] 基站退服数据支持（§4.3：退服基站列表 / 数量统计 / 趋势图）
-   [ ] 同步 `status/current.md`（图片清单 + 归属变更）

---

## 六、文档同步要求

| 触发动作                             | 必须更新                                                               |
| ------------------------------------ | ---------------------------------------------------------------------- |
| 模块命名变更（中文 / 目录 / 文件名） | `status/current.md` + `design/001-pm-output.md` + 本 task §10 实施记录 |
| 新增模块图片资源                     | `status/current.md`（图片清单）                                        |
| 结构型模块占位延续                   | `status/checklist.md` 注明"图片未到位，占位保留"                       |

---

## 七、实施记录

### 7.1 2026-08-25 — 网络影响按层级切换图片 + getter 改造

**触发**：用户提供 `网络影响-2/3/4/5.png`，要求从第一层到第五层用不同图片。

**改动**：

-   [modules/network-impact/index.tsx](../../../../web/pages/bj-cmcc-cmd-dispatcher/modules/network-impact/index.tsx)：
    -   新增 `NETWORK_IMPACT_IMG: Record<Level, { get src(): string }>` 映射，5 层级各对应一张图：
        -   `city` → `网络影响.png`
        -   `company` → `网络影响-2.png`
        -   `district` → `网络影响-3.png`
        -   `street` → `网络影响-4.png`
        -   `community` → `网络影响-5.png`
    -   路径基于 `constants.IMAGE_PATH`，用 getter 延迟取值（参考 `modules/map/presets.ts` 的 `MAP_LEVEL_ASSETS` 模式）。
    -   `station`/`logical` 占位空串（本模块不展示）。
    -   组件内 `src = NETWORK_IMPACT_IMG[currentLevel].src`。

**文档同步**：

-   `status/current.md`：模块说明、图片清单（5 张）、元信息日期已同步；本轮补正描述为 `Record<Level, { get src(): string }>` getter 映射。
-   本 task §3.1 / §4.1 / §4.2 / §五 看板：网络影响项均标记 ✅ 已完成。

**验证**：TS 诊断无报错。

### 7.2 2026-08-25 — 基站退服数据支持（趋势图）→ 分流到 task004

**触发**：PM 提供 `基站退服-full.png` 完整设计稿（日 / 周 / 月 / 自定义 tab + 折线图）。

**改动**：见 [done/task-2026-08-25-004-station-outage-chart.md](./done/task-2026-08-25-004-station-outage-chart.md) §10.1 实施记录。

**对本 task 影响**：
- §4.3 「基站退服数据支持」拆分为两项：趋势图（→ task004 ✅）/ 退服基站列表 + 数量统计（待后续）。
- §五 看板对应勾选更新。
- 文档元信息状态同步。

---

## 文档元信息

> 日期：2026-08-25（更新：task 收口，移入 plans/done/）
> 状态：图片接入 + 网络影响按层级切换 + PM 命名二次确认（已取消）+ 基站退服趋势图（→ task004）已完成
> 遗留（已分流到后续 task）：
> - 基站退服列表 + 数量统计（task003 §4.3）
> - 基站退服按层级切换图片（task003 §4.2）
> - 退服小区恢复情况按层级切换图片（task003 §4.2）
