# current-sprint.md · 本周任务索引

> **init 草稿 · 待显式确认后生效**（2026-09-04 init）
>
> 本周活跃任务的**索引**（仅放标题 + 链接，不放正文）；**≤ 50 行**。
> 单任务详情按需 `Read` 对应 `TASK-YYYY-MM-DD-NNN-<slug>.md`。

---

## 本周活跃任务（2026-W36）

<!-- 项目方按周维护 · 每行一条索引 -->

<!-- 新增任务时复制以下模板一行：
```
- [TASK-YYYY-MM-DD-NNN-<slug>](TASK-YYYY-MM-DD-NNN-<slug>.md) — @<owner> — <进行中/待开始/待 review>
```
-->

📋 任务完成后：先**同步更新所有引用**（current-sprint / backlog / roadmap），再将 `TASK-*.md` 移入 `done/<YYYY-MM>/`，最后在此处删除索引行（详见 `env/rules/03-review-discipline.md` 与 `env/AGENTS.md §5`）。

�� **禁止**：先移动文件再更新引用 —— 会造成路径断链与历史不一致。

---

## 本周已完成（待归档）

