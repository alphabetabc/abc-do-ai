# references/ · 稳定参考资料

> **定位**（§2）：稳定参考资料、名词解释、协作规范等长期维护文档；不是运行时状态。
>
> **何时使用**：L3 深度资料层（§3.1.3），仅在用户/Agent 明确要求或 Pointer Map 指向时按需 `Read` 单文件；**不被 `load` 默认扫描**。
>
> **何时新增**：项目里有跨 sprint / 跨年度复用的参考资料，且不属于 `design/` 意图层时。
>
> **何时不新增**：
> - ❌ 任务跟踪 → 进 `plans/`
> - ❌ 业务决策 → 进 `design/`
> - ❌ 项目硬约束 → 进 `env/AGENTS.md`

---

## 当前文件清单

| 文件 | 角色 |
| --- | --- |
| （按需） | 名词解释、协作规范等长期参考 |

<!-- 项目方按需追加 -->
